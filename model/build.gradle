plugins {
    alias(libs.plugins.android.library)
    alias(libs.plugins.kotlin.android)
    alias(libs.plugins.ktlint)
}
apply from: "../common.gradle"

android {
    namespace "de.danoeh.antennapod.model"

    lint {
        disable 'ParcelClassLoader'
    }
}

dependencies {
    annotationProcessor libs.androidx.annotation
    implementation libs.androidx.media
    implementation libs.commons.lang3

    testImplementation libs.junit
    testImplementation libs.androidx.test.core
    testImplementation libs.mockito.core
    // Disclosed, one-milestone exception to :model's Robolectric-free precedent (kotlin milestone 6):
    // needed to exercise real android.os.Parcel/Bundle round-trips for the Tier C Parcelable cluster
    // (DownloadRequest/FeedMedia/RemoteMedia) under plain JUnit. Do not broaden usage beyond Parcel
    // characterization tests for this milestone's four files.
    testImplementation libs.robolectric
}
