# Checkpoint — antennapod-fix-feeditemlistfragmenttest-flaky-characterization-tests

> **Repo:** `antennapod`
> **Task file:** `tasks/antennapod-fix-feeditemlistfragmenttest-flaky-characterization-tests.md`
> **Sibling/parent task:** `antennapod-fix-feeditemlistfragment-null-feed-crash` (merged, PR #29) — this task fixes two deterministic failures in the characterization test file that fix added, discovered after José merged the PR ahead of its own CI-verification step completing.
> **Blocks:** Milestone 15b (`tasks/antennapod-sync-settings-before-screenshot-milestone-15b.md`, PR #22) — needs this merged for a reliably green `emulator-test` run.

## Status
**PLANNED (2026-08-18) — ready for `legacy-android-red-team`.** Branch `fix/feeditemlistfragmenttest-flaky-characterization-tests` off `origin/develop @ a6275e8f0` (includes PR #29).

Plan is 3 steps across 2 files. Step 1 fixes both matchers (test-file only, defects 2+3). Step 2 adds the two-line production `progressBar` hide plus an assertion pinning it (defect 1). Step 3 is a **mandatory, gating** CI-verification step — 3 full `emulator-test` runs, all three API legs completing in each, no merge recommended until it is done and recorded.

Research's remaining Unknowns are all resolved in the Plan: Unknown 1 by José's scope decision, Unknown 2 on structural grounds (see below), Unknown 3 by step ordering, Unknowns 4 and 5 into Out of Scope.

Research materially revised the problem statement. **There are three defects, not two, and the two named in the pre-research are each API-level-specific rather than universal:**
1. **45s launch timeout — API 23 only.** The progressBar theory is **confirmed**, including observationally: the CI `test-report` artifact's view-hierarchy dump shows the fragment's spinner at `visibility=VISIBLE, 48×48` on the missing-feed path vs `visibility=GONE, 0×0` on the existing-feed path. The API 23 message's own idle timestamps are 6 ms apart across a 45 s window, proving the main thread stopped idling. The gap **pre-dates PR #29** (confirmed against `8443366a4`) and is not a regression from it. API-23-only scoping is explained by `AnimatedVectorDrawable` moving to the RenderThread at API 25.
2. **`withText(feed.getTitle())` ambiguity.** Colliders identified by name — **not the toolbar**: the nav-drawer subscription row (`nav_listitem.xml:30`, `NavListAdapter.java:239`) and the episode row's cover placeholder (`feeditemlist_item.xml:62`, `EpisodeItemViewHolder.java:95`). 2 matches on API 23, **3** on API 30/36.
3. **NEW, not in the pre-research: `withId(R.id.txtvTitle)` matches 12 views** at `FeedItemlistFragmentTest.java:56`. This is what `testMissingFeedShowsEmptyStateWithoutCrashing` actually fails on for API 30 and 36 — it launches fine there. Ten of the twelve are nav-drawer rows. **Fixing defect 1 alone would just convert the API 23 leg from a timeout into this failure.**

Also established: nothing here is intermittent (8/8 completed CI attempts identical per API level — "flaky" in the slug is a misnomer); these two tests are the **only** failures left in the whole suite (`DownloadLogTest` passed on all 8 attempts, so PR #29's production fix is working); and `FeedSettingsTest` is a genuine controlled comparison — same `EXTRA_FEED_ID` launch, passes, because its feed exists (success path hides the spinner) and its title matcher is ancestor-scoped via `isDescendantOfA(withId(R.id.appBar))`.

## Last updated
2026-08-18

## Lifecycle progress
- [x] Branch created off `origin/develop @ a6275e8f0`, parent-task checkpoint bookkeeping committed (`4efd6f528`)
- [x] Research (legacy-android-researcher) — 2026-08-18; three defects found, not two; progressBar theory confirmed observationally; colliding views named
- [x] Plan (legacy-android-planner) — 2026-08-18; 3 steps, 2 files, all three defects sequenced; Unknown 2 resolved structurally; CI verification made a gating step
- [x] Red-team plan (legacy-android-red-team) — 2026-08-18; **APPROVE**, Loop 1 of max 2. Independently verified (not taken on faith): the `withParent(withId(R.id.coordinatorLayout))` progressBar anchor by reading `feed_item_list_fragment.xml` and confirming its two named collision layouts (`nav_list.xml`, `feeditemlist_item.xml`) genuinely have different immediate parents; the `isDescendantOfA(withId(R.id.appBar))` matcher by confirming `appBar` is declared in exactly 2 layouts repo-wide and only 1 `txtvTitle` sits under it in this fragment's layout, plus confirming no `layout-v*` qualifiers exist anywhere (only `layout-sw720dp/main.xml`, no `appBar`); the two-line production fix's insertion point and File Scope against the current `FeedItemlistFragment.java`; and no interaction between the new progressBar-hide lines and the parent task's `MaybeCallbackObserver`/`localFeed` concurrency finding (that finding is scoped to the *success* consumer only; this task's lines are in `error`/`onComplete`). One MINOR nit filed (D3's wording implies more of `FeedSettingsTest`'s idiom was copied than actually was — conjuncts match, wait-mechanism deliberately doesn't) — does not block. Full verdict in the task file.
- [x] Implement (android-migration-developer) — 2026-08-18; Steps 1 and 2 both complete, two separate commits (`734c0971a` test-only matchers, `ecd85e9ca` production fix + assertion). `./gradlew :app:assembleDebug` PASS (both flavors), `./gradlew checkstyle lint` PASS. Both `FeedItemlistFragmentTest` tests run and PASS on a real, locally-booted API 36 emulator (`Pixel_9a` AVD), both before and after Step 2 — device wasn't available during Research but became available here, so this is real instrumented-test execution, not just compile verification. API 23/30 not locally reproducible (no AVD for those levels); remain for Step 3's mandatory CI runs. Full details in the task file's Implementation Notes.
- [ ] Code review (migration-code-reviewer)
- [ ] Red-team implementation (legacy-android-red-team)
- [ ] PR opened
- [ ] CI verification (3 full `emulator-test` runs, per this task's own Plan — do not merge before this completes, given what happened to the parent task)
- [ ] PR merged
- [ ] Milestone 15b's branch rebased/re-checked once more for a genuinely, reliably green `emulator-test` run

## Decisions for next session
- Discovered 2026-08-18 during PR #29's post-merge CI verification (see parent task's checkpoint for full detail) — José merged that PR before its CI-verification step finished, so this follow-up exists to close the gap on `develop` rather than on a pre-merge branch.
- ~~Test-file-only fix expected; do not expand File Scope to production code~~ — **superseded 2026-08-18.** José decided Research Unknown 1 in favour of also fixing production: add `viewBinding.progressBar.setVisibility(View.GONE);` to `FeedItemlistFragment.loadItems()`'s `error` and `onComplete` handlers, on the grounds that pre-API-25 users hitting a missing/deleted feed currently see a permanently spinning progress bar — a real UX gap, not merely an Espresso-idle artifact. The decision is recorded verbatim above the Plan section in the task file. **File Scope is therefore 2 files, not 1**, and is capped there: exactly two added lines in the production file, nothing else in it.
- **The plan must address all three defects.** Defect 3 (`withId(R.id.txtvTitle)` → 12 views at `:56`) is currently masked on API 23 by defect 1. Fixing only defect 1 moves the API 23 leg from timeout to ambiguity failure, not to green.
- Acceptance question changed: not "did the flake stop?" (nothing is intermittent — 8/8 identical attempts) but "do both tests pass on API 23, 30 and 36?" — answerable in a single CI run.

## Planner's decisions (2026-08-18) — read before red-teaming

- **Matchers are scoped by ancestor, not by visibility.** `:56` → `allOf(isDescendantOfA(withId(R.id.appBar)), withId(R.id.txtvTitle))`; `:76` → `allOf(isDescendantOfA(withId(R.id.appBar)), withText(feed.getTitle()), isDisplayed())`, the `FeedSettingsTest.java:59-60` idiom verbatim.
- **Research Unknown 2 resolved without an extra verification step, on structural grounds.** `R.id.appBar` is declared in exactly 2 layouts repo-wide (`feed_item_list_fragment.xml:11`, `feedinfo.xml:11` — the latter a different screen, never concurrent), and there is exactly one `txtvTitle` under it. There are **no `layout-v*` qualifiers anywhere in the repo** (the only layout qualifier is `layout-sw720dp/main.xml`, a screen-size bucket declaring no `appBar`), so containment is identical on API 23/30/36. The API-level variation Research documented is entirely in visibility/presence state — the axis ancestor-scoping does not consult. Residual risk stated in the Plan: `:56` has never executed on API 23, so Step 3's run is its first-ever execution there.
- **`isDisplayed()` deliberately omitted at `:56`** (kept at `:76`). At `:56` the assertion is `withText("")`, and an empty `TextView`'s bounds are the most API-variable thing in play; containment is already unique, so the conjunct could only convert a correct assertion into a `NoMatchingViewException`.
- **`waitForViewGlobally` kept at `:76`, not swapped for `waitForView`.** `EspressoTestUtils.waitForView:74-80` returns on the first breadth-first match, so an over-broad matcher would silently pass against the wrong view; `waitForViewGlobally` routes through `onView` and throws `AmbiguousViewMatcherException` loudly. Loud failure beats a possible silent false green on a characterization test. Also the smaller diff.
- **The new `progressBar` assertion needed scoping too — `withId` alone would have reintroduced the same class of bug.** `id/progressBar` appears in **14** layouts, including `nav_list.xml:77` (always-present drawer) and `feeditemlist_item.xml:199` (every episode row). Anchor is `withParent(withId(R.id.coordinatorLayout))`, not `isDescendantOfA`: the fragment's spinner is a *direct child* of `coordinatorLayout` while episode-row spinners are descendants of it. `R.id.coordinatorLayout` is declared in exactly one layout repo-wide.
- **The "characterization tests first" rule is satisfied in inverted form, not waived.** The tests exist but cannot execute, so Step 1 (repair until they execute and pin behavior) is the characterization step and precedes all production code. Stated openly in the Plan: Step 2 has no honest "pin current behavior / prove equivalence" framing, because current behavior on that path *is* the defect — it is a deliberate user-visible behavior change, and the D6 assertion is a regression test for the new behavior. Step 1's CI evidence (API 23 still failing at launch with both matchers fixed) serves as the "red before".

## Resume command
Implementation complete 2026-08-18 (see task file's Implementation Notes section). Both commits on branch: `734c0971a` (test-only matcher fixes, Step 1), `ecd85e9ca` (production progressBar fix + new assertion, Step 2). Not pushed, PR not opened. Invoke `migration-code-reviewer` on `tasks/antennapod-fix-feeditemlistfragmenttest-flaky-characterization-tests.md` next. Step 3 (mandatory 3x CI-verification, gating merge) still follows code review + implementation-stage red-team + PR open, per the Plan — not yet done.

Two things the red-team probed hardest and confirmed sound: (1) the Unknown 2 resolution — independently verified the ancestor-scoped matchers depend only on static layout containment (confirmed no `layout-v*` qualifiers exist anywhere in the repo except `layout-sw720dp/main.xml`, which declares no `appBar`), so the "no extra pre-ship verification needed" argument holds; (2) Step 3's gating status — confirmed still an explicit, blocking Acceptance Criteria checkbox in the Plan, unchanged. Open Question 2 still pre-commits the response if API 23 still times out after Step 2: stop and report, do not widen scope.
