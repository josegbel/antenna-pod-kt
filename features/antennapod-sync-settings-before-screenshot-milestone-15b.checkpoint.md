# Checkpoint — antennapod-sync-settings-before-screenshot-milestone-15b

> **Repo:** `antennapod`
> **Task file:** `tasks/antennapod-sync-settings-before-screenshot-milestone-15b.md`
> **Depends on:** Milestone 15 (PR #21, merged into `develop` at `f5d4c5551`, 2026-08-06).

## Status
**AC13 RESOLVED WITH REAL CI EVIDENCE (2026-08-20) — ready to re-invoke `migration-code-reviewer`.** The pre-existing `static-analysis` debt that blocked AC13 (see below) was cleared by five sibling follow-up tasks, all merged into `develop` since this checkpoint was last updated. This branch was merged with `origin/develop` (merge commit `412068b6d`, clean, no conflicts, no force-push) and pushed, triggering CI run `32348999145`: **`static-analysis` passed for the first time ever on this PR**, `unit-test`/`emulator-test` ran for real and passed on every variant/API leg, and all three `SyncSettingsScreenshotCaptureTest` methods **PASSED on `ubuntu-latest`** — including an exact-match confirmation that the captured bitmap is **822×1782** on Linux CI (resolving Research's cross-machine-determinism gap). One honest residual: the distinct-colour-count assertion is a `>= 200` threshold with no `println`, so the exact observed number was never captured anywhere retrievable — AC13's literal "paste the count" ask can't be fulfilled without a one-line test change, which was not made (out of this session's scope to decide). Full evidence in the task file's AC13 entry, "RESOLVED 2026-08-20" block.

_Prior status (superseded): Implementation done, Implementation Notes written, PR #22 open. AC13 (CI-gate) cannot be satisfied in its literal form — disclosed, not worked around — because CI's `unit-test` job is gated behind a `static-analysis` job that fails on pre-existing, unrelated SpotBugs debt in `:app`. Ready for code review with that one open item flagged for José._

_Also superseded — a code review pass did in fact run after this checkpoint entry was written (commit `2f075df01`, not reflected here until now): `migration-code-reviewer` gave **REQUEST CHANGES** on exactly this AC13 gap, calling it the one open item blocking sign-off. That is the finding this session's CI evidence now addresses._

## Last updated
2026-08-20

## Lifecycle progress
- [x] Research (legacy-android-researcher)
- [x] Plan (legacy-android-planner)
- [x] Red-team plan (legacy-android-red-team) — **Loop 1: CHALLENGE.** CRITICAL: `SyncSettingsTestHost` used the wrong theme (`Theme_AntennaPod_Light` vs. production's real `Theme.AntennaPod.Light.NoTitle` via `ToolbarActivity`), so the captured chrome wouldn't match what ships. Also 2 MAJOR (the 200-color floor doesn't alone catch the sdk-36 clipping failure; writing only to gitignored `build/` doesn't fully address CI-environment risk) and 1 MINOR (D1's dirty-file handling didn't account for #21 already merging). Revised: rather than editing the shared host (which would've broken 4 of Milestone 15's 38 tests that assert on `supportActionBar`, and — a deeper bug the planner found on its own — naively reusing `android.R.id.content` on a real `ToolbarActivity` would attach the fragment as an overlay sibling of the toolbar due to a content-id collision in `toolbar_activity.xml`, producing a screenshot that looks plausible but is structurally wrong), the plan now adds a dedicated `SyncSettingsCaptureHost` extending `ToolbarActivity` and inflating production's real `settings_activity.xml`. **Loop 2 (final): APPROVE** — every claim in Revision 1 independently re-verified against source, including the content-id collision and the four at-risk Milestone 15 tests. One MINOR non-blocking note (theme-qualifier resolution asserted by inspection, not empirically probed — self-correcting via Step 1's visual-inspection gate). Full verdicts at `## Red-Team Verdict — Plan` in the task file.
- [x] Implement (android-migration-developer) — branched `test/ui-preferences-sync-settings-before-screenshot` from fresh `develop` (after stashing the two M15 doc leftovers per revised D1 — stash ref `stash@{0}`, "M15 spec leftovers (see 15b D1)", verified still intact and unneeded for this task's own diff). 4 commits: Step 1 (new `SyncSettingsCaptureHost` + screenshot test), Steps 2-3 (checked-in artifact + README convention entry), plus two empty CI-retrigger diagnostic commits (disclosed in Implementation Notes, no file changes). Branch pushed, **PR #22 opened**: https://github.com/josegbel/antenna-pod-kt/pull/22. Implementation Notes section now written in full in the task file, with every Acceptance Criterion independently verified and recorded (not just claimed).
- **RESOLVED (was BLOCKED) — CI stall explained.** The retrigger commit (`8203a70d7`) did get a real `Checks` run (`31159097992`) after the earlier ~hour-long silence. That run failed, but the `unit-test`/`emulator-test` jobs (which would run the actual screenshot test) both declare `needs: static-analysis` (`.github/workflows/checks.yml:58,109`), and `static-analysis` failed on 7 pre-existing SpotBugs findings entirely outside this PR's diff (`QueueFragment.java` ×2, `PreferenceActivity.java`, `OnlineFeedViewActivity.java`, `FeedInfoFragment.java`, `MainActivity.java` ×2 — confirmed via `git diff --name-only f5d4c5551..HEAD` that none of these files are touched). AC13's literal CI-gate requirement is therefore structurally unreachable right now, through no fault of this implementation — documented in Implementation Notes rather than claimed as passing or silently dropped.
- [x] Code review (migration-code-reviewer) — **REQUEST CHANGES**, Loop 1 (commit `2f075df01`, date not re-derived here — read the task file's Code Review Verdict section directly). One finding: AC13 not satisfied (real CI run never executed the screenshot test, blocked by upstream `static-analysis` debt unrelated to this diff). Not a defect in the diff itself — the reviewer's own words: "the requested 'change' is not more code — it is getting the José decision the developer already correctly identified as needed, recording it, and re-invoking."
- **AC13 gap resolved with real CI evidence (2026-08-20)** — see Status above and the task file's "RESOLVED 2026-08-20" block under the AC13 entry. Merged with `origin/develop` (`412068b6d`), CI run `32348999145` green across the board, `SyncSettingsScreenshotCaptureTest` passed for real on Linux CI for the first time. One honest residual (exact distinct-colour count not printed by the test, only the `>=200` threshold confirmed) flagged for José rather than fabricated.
- [x] Re-invoke code review (migration-code-reviewer) — **APPROVE**, Loop 2 (2026-08-20). Independently re-verified from raw sources, not the report: `gh run view 32348999145 --log --job=96364870100` shows the three `SyncSettingsScreenshotCaptureTest` methods with literal `PASSED` lines; test source confirms `assertEquals(822/1782, ...)` (exact-match, not range) and `assertTrue(distinctColors.size >= 200)` (a real content assertion, no `println` for the exact count — the disclosed AC13-text gap is accurate and judged non-blocking, a MINOR); `git diff --stat origin/develop HEAD` confirms exactly the 7 File Scope files with no drift from the merge. Two MINOR findings recorded, both non-blocking: (1) the exact distinct-colour figure remains unprintable without a follow-up one-line test change, (2) this checkpoint's own "Resume command" section (below) is still stale from Loop 1 and was never fixed. Also surfaced: PR #22's *newest* CI run (`32351500729`, triggered by the docs-only AC13-resolution commit) currently shows `Unit Test: *` failing at the `Build` step on `:app-wearos:produceFreeReleaseComposeMapping` — confirmed unrelated to this task (module untouched by the diff; `develop`'s own contemporaneous runs are green), consistent with transient CI flakiness rather than a regression. Full verdict at `## Code Review Verdict` (Loop 2) in the task file.
- [x] Red-team implementation (legacy-android-red-team) — **APPROVE, Loop 1 of max 2 (2026-08-20).** Independently re-verified from source and live systems, not from prior stages' reports: confirmed the content-id collision and `SyncSettingsCaptureHost`'s genuine chrome fidelity by reading `ToolbarActivity.java`/`toolbar_activity.xml`/`settings_activity.xml`/`PreferenceActivity.java` directly; ran the three screenshot tests locally (all PASS) and independently cross-checked the rendered PNG with PIL (364 distinct colours, 822×1782, byte-identical `shasum` to the checked-in artifact); re-fetched and diffed `origin/develop` against `HEAD` (exactly the 7 File Scope files, 0 production files touched); queried `gh pr checks 22` and pulled the current run's raw log, confirming `SyncSettingsScreenshotCaptureTest`'s three methods pass on the PR's present green CI state. Two MINOR, non-blocking findings: (1) the distinct-colour exact figure remains unprintable by the test itself, though independently reproduced as 364 in this review; (2) this checkpoint's own stale "Resume command" section (fixed in this same update). Full verdict at `## Red-Team Verdict — Implementation` in the task file.
- [x] PR opened — https://github.com/josegbel/antenna-pod-kt/pull/22. Code review APPROVE (both loops) and implementation red-team APPROVE. PR is fully green on CI (`gh pr checks 22`, run `32351500729`, all jobs pass). **Ready for a human merge decision** — this agent pipeline does not merge PRs itself.

## Decisions for next session
- Small, test-only addition discovered mid-conversation: automate the "before" screenshot for the eventual Milestone 20 Compose comparison, using the Robolectric fragment-driving harness (`SyncSettingsTestHost`) that Milestone 15 already built, instead of a manual `adb` capture or waiting on Paparazzi (Milestone 16).
- **Update 2026-08-06: PR #21 merged into `develop` (`f5d4c5551`).** Branch fresh from `origin/develop` as normal — the stacked-branch plan is no longer needed. Verify via fresh fetch + ancestry check first (Milestone 15's D1 precedent: local `origin/develop` refs have been stale before).
- José authorized this 2026-08-06, explicitly asked for it to be a separate PR rather than added to #21.
- Auto-chain: not yet explicitly re-confirmed for this specific task by José in-session (see [[antennapod-pipeline-autonomy]] memory — authorization doesn't silently carry forward to a new milestone). The orchestrating session stated its intent to auto-chain this one too, consistent with the spirit of the standing instruction and the small/low-risk size of this task, giving José the chance to redirect.

## Resume command
This task's lifecycle is **complete** through implementation red-team. Research, Plan (twice red-teamed, APPROVE), Implementation, Code Review (twice looped, APPROVE), and Implementation Red-Team (APPROVE) are all done in `tasks/antennapod-sync-settings-before-screenshot-milestone-15b.md`. Revision 1 raised the plan's real counts to **thirteen acceptance criteria and seven File Scope files** (not the original twelve/six).

**Next step: a human merge decision on PR #22** (https://github.com/josegbel/antenna-pod-kt/pull/22) — this pipeline does not merge PRs itself. PR is fully green on CI as of 2026-08-20 (`gh pr checks 22`, run `32351500729`). Two disclosed MINOR items on record (both non-blocking): the distinct-colour-count exact figure isn't printed by the test itself (independently reproduced as 364 during the implementation red-team pass), and OQ1/OQ2 (artifact's final publishing home; licensing/attribution before any public use) remain open for José and gate anything *after* merge, not the merge itself.
