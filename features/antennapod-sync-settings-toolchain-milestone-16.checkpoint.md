# Checkpoint — antennapod-sync-settings-toolchain-milestone-16

> **Repo:** `antennapod`
> **Task file:** `tasks/antennapod-sync-settings-toolchain-milestone-16.md`
> **Depends on:** Milestone 15 (`kotlin` track, PR #21, merged into `develop` at `f5d4c5551`, 2026-08-06). **Milestone 15b (PR #22) is also now merged into `develop`, as of 2026-08-20 12:41:58 +0200 (commit `45904a410`) — discovered during the Loop 2 red-team pass, after Research had recorded it as unmerged. Rebased into the Plan by Revision 3; the branch for this milestone is cut fresh from `origin/develop`, which contains both.**

## Status
**IMPLEMENTATION COMPLETE — 2026-08-27. Shipped without Paparazzi (D6's stop condition fired in substance, José's decision, 2026-08-27). Hand off to `migration-code-reviewer` next.** Steps 1–9 and 13 landed exactly as planned (five commits: Steps 2, 3, 6, 7, 8; Steps 1/5 no-diff, folded into evidence; Steps 4/9 empty, checked). Steps 10–12 (Paparazzi) were attempted at Step 10 and found build-breaking for a reason outside D6's own remedy ladder — see `tasks/antennapod-sync-settings-toolchain-milestone-16.md` → `## Implementation Notes` for the full account (an unpublished `aapt2` artifact reference inside Paparazzi `2.0.0-alpha05`'s own transitive `sdk-common:31.13.2`, breaking buildscript resolution repo-wide the instant the plugin was declared `apply false` at root — not the Robolectric/layoutlib JVM-sharing conflict the ladder was built for). Nothing was ever committed for Step 10, so no revert commit was needed; the working tree returned directly to the Step 8 commit. AC25's disposition table applied to every criterion that assumed Paparazzi shipped. Full acceptance-criteria table, the D13 before/after dependency diff, and every test command run are in Implementation Notes. `tasks/antennapod-sync-settings-modernization-future-work.md`'s Milestone 20 section carries the specific finding forward (check whether a later Paparazzi release fixed its own `sdk-common` pin before re-attempting, not the same ladder).

**Prior status, retained as history:** READY FOR IMPLEMENTATION — Plan Revision 3 (baseline correction after the Milestone 15b merge) written 2026-08-20. The Loop 2 blocker is closed; handed off to `android-migration-developer`. José authorised the narrow out-of-cycle correction (option (a) of the Loop 2 escalation) rather than a third red-team loop — **the max-2-loop rule stands and is not being circumvented: Revision 3 is a factual rebase, not a new design surface.** No decision reopened, no track changed, File Scope identical, all 29 acceptance criteria intact. Auto-chain authorization **resumes** from here. Scaffolded 2026-08-20. José resolved the one carried-over open question (Hilt's `:app` footprint — accepted) before scaffolding, and authorized auto-chaining this milestone's pipeline stage-to-stage to PR without pausing for confirmation between stages. Research finished 2026-08-20 (11 unknowns). Plan finished 2026-08-20: all 11 unknowns resolved as D1–D14, 10 ordered Steps, explicit File Scope, 22 acceptance criteria, 5 open questions.

**Plan — Revision 1 written 2026-08-20, responding to José's answers on OQ1/OQ2/OQ5.** D4 and D6 rewritten in place; Steps 10 → **13**; acceptance criteria 22 → **26**; File Scope gains its first `Created` entries. **Paparazzi is no longer deferred — it is in scope, mitigated and proven.** The Compose BOM moved to the newest release this stack can physically accept, which is *not* the one José named. Every version pin was re-resolved against the live repositories in this revision, not carried over. No code written; no branch cut.

**Red-Team Verdict — Plan, Loop 1 of 2, 2026-08-20: CHALLENGE.** Four findings, three MAJOR and one MINOR — none require re-scoping the milestone or reopening D4's BOM-floor logic or D6's Paparazzi-mitigation choice, both of which held up under independent re-verification against the actual repo. See `tasks/antennapod-sync-settings-toolchain-milestone-16.md` → `## Red-Team Verdict — Plan` for full evidence. Summary:
1. **MAJOR** — "34 modules" (actual: **37**) and "14 Robolectric modules" (actual: **15**) are stated as measured fact in D3, D6 rung 4, D11 rung 3, D14, File Scope and the Milestone section, and are wrong — verified directly via `settings.gradle`'s include count and `grep -rl common.gradle`/`libs.robolectric` across `build.gradle` files. Doesn't change any decision's substance, but undermines the Plan's own "measured, not recalled" claim.
2. **MAJOR** — D6/Step 12's stop condition says "revert Steps 10–11" but Step 12 itself adds ladder-rung commits (rungs 2–4) that aren't covered by that phrase; and no AC is marked void if the stop condition fires (AC14/AC19/AC20/AC21/AC23/AC24 all assume Paparazzi ships).
3. **MAJOR** — The one production-source change (`@HiltAndroidApp` on `PodcastApp.java`, which ships in the real R8-minified release APK) is verified only by build-success (AC4) and Robolectric/JVM-level tests (AC9) — nothing installs/launches the actual R8-minified build, despite `AGENTS.md`'s own default verification practice being exactly that, and despite R8-interaction being a named risk category for this review.
4. **MINOR** — AC2b's "Step 7 stops" framing implies a live Gradle resolution check that can't happen until Step 8 (catalog entries are inert until consumed, per the Plan's own gap #3) — needs a sequencing clarification, not a substantive fix.

**Plan — Revision 2 written 2026-08-20. All four Loop 1 findings closed; no decision reopened, no track dropped, File Scope unchanged, no new commit and no new file in the developer's diff.** What changed:
1. **New D15** records the measured counts — **37 modules** (`settings.gradle` includes; all 37 apply `common.gradle`), **15 Robolectric modules**, **34** `annotationProcessor` modules, **35** other-module `build.gradle` files — and identifies where `34` came from: it is the real `annotationProcessor` count, mislabelled "one per module" in Research (`:186`) and propagated from there. Eleven Plan occurrences corrected in place, plus both occurrences inside the Revision 1 narrative (each marked as a Revision 2 number-only correction). Research's prose is deliberately left verbatim and superseded by recorded correction, matching how D4/D5/D7/D9 handle the other four corrections-to-Research. A pre-existing `app/build.gradle:36-38` → `:37-38` citation error was caught in the same sweep and fixed.
2. **D6's stop condition now reverts Steps 10, 11 *and* 12** — every attempted ladder rung included, with each rung's stranded artifact named. **New AC25** governs the shipped diff in that case and gives every affected criterion an explicit status: VOID (AC2c, AC24, AC21's Paparazzi clause, AC22's Paparazzi README convention), CHANGED MEANING (AC23 → failed-experiment evidence record; AC19 → inverts back to zero hits), NARROWED (AC14, AC11's addition clause, AC7 back to 38), RE-ANCHORED (AC4/AC8/AC15's Step-11 checkpoints → Step 13). **AC20 is explicitly confirmed *unaffected*** — it is KSP-only, so the red-team's inclusion of it is corrected by name rather than by silence.
3. **New AC26 + AC27** close the install/launch gap. AC26 installs and launches at Step 3 *and* Step 13, in both `installPlayDebug` (AGENTS.md's own step) and **`installPlayRelease`** — the R8-minified, shrunk, signed APK where the risk lives — with pass conditions of install success + live `pidof` + clean `logcat`, and `monkey`'s exit code explicitly rejected as evidence because `PodcastApp.java:18`'s crash handler swallows failures. AC27 captures `mapping.txt` + dex evidence that `Hilt_PodcastApp` survived shrinking, **in addition to and never instead of** AC26. No device available ⇒ escalate to José, never waive. D12 gains the block explaining why AC4 (R8 ran) and AC9 (unshrunk JVM classpath) do not cover this.
4. **AC2/AC2b split into Half A (Step 7, direct AAR download + `aar-metadata.properties` read, outside Gradle) and Half B (Step 8, live AGP-enforced confirmation)** — because a catalog entry no module consumes is inert. In both halves the remedy for a floor violation is a different BOM pin, never a `compileSdk`/AGP bump.

Acceptance criteria **26 → 29**. Steps stay at **13** (no renumbering).

**Plan — Revision 3 written 2026-08-20 (baseline correction after the Milestone 15b merge). Closes the Loop 2 finding. Nothing else changes.** Verified independently against the real remote before writing: `origin/develop` tip is `45904a410` (Milestone 15b, PR #22), `f5d4c5551` is still an ancestor, and the `:ui:preferences` sync-settings baseline is **11 files / 8 test classes / 41 `@Test` methods** — Milestone 15's 9/7/38 plus 15b's `SyncSettingsCaptureHost.kt` (support) and `SyncSettingsScreenshotCaptureTest.kt` (3 tests). What changed:
1. **AC7 rebased, logic untouched.** Per-class table 7 classes/38 → **8 classes/41** (adds ScreenshotCapture 3); totals "38 through Step 8, 39 from Step 11" → "**41** through Step 8, **42** from Step 11". The criterion still tests exactly what it tested — every existing test passes, no existing per-class count moves in either direction, total rises by exactly one at Step 11 — because this milestone still changes no screen behavior and still adds only Step 11's Paparazzi proof test.
2. **D1's reasoning corrected.** Its "no coordination with PR #22 is required" rested on "since D6 defers Paparazzi entirely," a premise Revision 1 voided when it brought Paparazzi into scope. Replaced: 15b **is merged**, so the branch is cut fresh from an `origin/develop` containing both Milestone 15 and 15b — **no unmerged sibling branch, no rebase ordering, no pending merge decision. Confirmed explicitly as a simplification, not a complication.**
3. **D6 gains a Revision 3 note, no decision change.** 15b's `@GraphicsMode(NATIVE)` test is now unconditionally in the JVM Step 11's coexistence experiment runs in, making it a *harder* proof of `forkEvery` (layoutlib's native path driven from both sides in one task) rather than a compromised one. `forkEvery = 1` caps each JVM at one class regardless of count, so the mechanism is unaffected. Research's Unknown 10 closes as checked fact.
4. **Fourteen other baseline references corrected** (Objective, D5 ×2, D6 ×4, D12, D14, Steps preamble, Steps 1/6/11/12, File Scope, AC11, AC25 ×2, "Not applicable" paragraph, Milestone section), plus two knock-ons the red-team did not flag: D6 rung 2's fidelity cost is now "38 of the 41" (15b's 3 already pin `sdk = [34]`), and **Step 13's README conventions are numbered from 10** because 15b brought convention 9 onto `develop`.
5. **Research, the pre-research context, and the Revision 1/2 narratives are left verbatim** and superseded by recorded correction — the D4/D5/D7/D9/D15 convention. The reason is stronger here: unlike D15's module count, those figures were **correct when written**; `develop` did not contain 15b yet. No D16 was added, because nothing was inferred wrongly — a fact changed after measurement.

## Last updated
2026-08-27 (PR #31 opened against `develop` — milestone complete: code review APPROVE, implementation red-team APPROVE on Loop 2/2 final after one docs-only fix cycle)

_Prior: 2026-08-27 (Implementation complete — shipped without Paparazzi, D6 stop condition fired in substance, José's decision 2026-08-27; status → ready for code review)_
_Prior: 2026-08-20 (Plan Revision 3 — baseline rebased to 11 files / 41 tests after Milestone 15b merged; status BLOCKED → ready for implementation)_

## Lifecycle progress
- [x] Research (legacy-android-researcher) — 2026-08-20
- [x] Plan (legacy-android-planner) — 2026-08-20
- [x] Plan — Revision 1 (legacy-android-planner) — 2026-08-20, responding to José's OQ1/OQ2/OQ5 decisions
- [x] Red-team plan (legacy-android-red-team) — 2026-08-20, **Loop 1 of 2: CHALLENGE** — 3 MAJOR + 1 MINOR findings, see above
- [x] Plan — Revision 2 (legacy-android-planner) — 2026-08-20, all four Loop 1 findings closed; D15 added, AC25/AC26/AC27 added, 26 → 29 criteria
- [x] Red-team plan, Loop 2 of 2, FINAL (legacy-android-red-team) — 2026-08-20, **CHALLENGE — escalated to José, no Loop 3.** All four Loop 1 findings independently re-verified and confirmed genuinely closed (counts, AC25's completeness, AC26/AC27's technical soundness, AC2b sequencing). One new MAJOR finding: `origin/develop` moved since Research ran — Milestone 15b (PR #22) merged into `develop` today (`45904a410`, 2026-08-20 12:41:58 +0200), which Research had recorded as unmerged. This makes the module's real test baseline on `develop` **41 tests across 11 files** (not the Plan's stated 38/9) and falsifies D1's "no coordination with PR #22 required" conclusion, which was already stale on its own terms (built on "D6 defers Paparazzi entirely," reversed in Revision 1) independent of the branch state. AC7 as literally written (38→39 totals) cannot pass against the real repo. Full evidence and suggested corrections in `tasks/antennapod-sync-settings-toolchain-milestone-16.md` → `## Red-Team Verdict — Plan (Loop 2, Final)`. AC26's device/emulator dependency was reviewed and found properly handled (escalation, not silent waiver) — one MINOR note that the plan doesn't say whether "escalate to José" pauses auto-chain or just flags the PR, not blocking.
- [x] **José's decision, 2026-08-20: option (a)** — planner authorised to make the narrow baseline correction out-of-cycle. **No third red-team loop; the max-2-loop rule stands.** Auto-chain resumes.
- [x] Plan — Revision 3 (legacy-android-planner) — 2026-08-20, baseline rebased 9 files/38 tests → **11 files / 41 tests** after Milestone 15b's merge; AC7 and D1 corrected, D6 annotated, fourteen other references swept. No decision reopened, File Scope unchanged, 29 criteria unchanged.
- [x] Implement (android-migration-developer) — 2026-08-27, shipped without Paparazzi (D6 stop condition fired in substance; José's decision 2026-08-27); six commits total once Step 13 landed (`316481c2e`, `1e387562a`, `766aaf993`, `78f13f047`, `59287054f`, `37ca31a4a`); full account in Implementation Notes
- [x] Code review (migration-code-reviewer) — 2026-08-27, **APPROVE, Loop 1.** One MINOR non-blocking note (PodcastApp.java's diff is 3 lines not D8's literal "2," a defensible import+annotation stylistic choice). Independently re-ran the test suite (41/41 green), `:app:assembleDebug`, and fetched the live Compose BOM POM to confirm D4's material3/foundation versions.
- [x] Red-team implementation (legacy-android-red-team) — 2026-08-27, **Loop 1 of 2: CHALLENGE.** CRITICAL: AC5's D13 dependency diff carried a stale claim that `:app`'s `kotlin-stdlib` moved 2.3.20 → 2.3.21 (a leftover prediction for the originally-planned Hilt 2.60.1, never re-derived after the disclosed step-down to Hilt 2.58, which actually declares `kotlin-stdlib 2.2.20`) — re-verified live, `kotlin-stdlib` never moved. MAJOR: D6's four-rung remedy ladder was never annotated to record that it was built entirely for the Robolectric/layoutlib JVM-sharing conflict and never anticipated a buildscript-classpath failure class — the actual failure mode that preempted it at Step 10. Both are documentation-only findings; no shipped code was implicated. Developer fixed both in a docs-only commit (`40c8ef378`), independently re-verifying the correction (not just editing text) and sweeping the rest of the D13 table for other staleness (found and added one more accurate, previously-undocumented row: `material-icons-core`/`-android`'s removal via the material3 1.3.1→1.4.0 bump).
- [x] Red-team implementation, Loop 2 of 2, FINAL — 2026-08-27, **APPROVE.** Both Loop 1 findings independently re-derived from scratch and confirmed genuinely fixed; the new `material-icons-core` row independently checked against both material3 POMs and confirmed accurate; confirmed the fix commit touches only the task file, no code/build/test file. Closes the max-2-loop implementation red-team with no findings outstanding.
- [x] **PR opened** — 2026-08-27, [PR #31](https://github.com/josegbel/antenna-pod-kt/pull/31), `toolchain/sync-settings-milestone-16` → `develop`. Six commits, description follows the Milestone 15/15b precedent (no linked issue — portfolio work, "Closes: #xy" checklist item left unchecked to match).

**Milestone 16 is complete.** Next milestone in the sequence (17: concurrency+MVVM) should treat this PR's merge as its own dependency baseline, the same way this milestone treated Milestone 15b's.

## Decisions for next session
- José decided 2026-08-20: accept Hilt's `:app`-level footprint (Hilt plugin + `@HiltAndroidApp` on the Application class) rather than going non-Hilt/manual constructor injection, given the more representative "production Android DI" story it tells for a portfolio case study. This is a disclosed, intentional exception to the portfolio's usual single-slice-scoped diff pattern — record it as such in Implementation Notes, not as an out-of-scope finding.
- Auto-chain authorized for this milestone specifically, 2026-08-20.
- **José answered OQ1, OQ2 and OQ5 on 2026-08-20 — these are settled, do not relitigate:**
  - **OQ1 (Paparazzi):** *"if we can use Paparazzi 2.0 let's do that."* → Paparazzi is **in scope**. Revision 0's deferral is reversed.
  - **OQ2 (Compose BOM):** *"If we can migrate to todays Compose 2026.08.00 let's do that."* → Honoured as far as the stack allows; see the hard blocker below. The decision to take the newest *feasible* BOM rather than the newest BOM is the planner's, made on measurement, and is the one item a red-team should confirm it agrees with.
  - **OQ5 (upstreaming):** *"no worries as a standalone upstream PR, that's not a problem."* → Closed, no plan change. The milestone is **not** to be restructured or squashed with 17–18 for upstream palatability.
  - OQ3 (`kotlin-bom` deletion) and OQ4 (no dependency lock file) remain open; neither blocks.
- This milestone changes no Sync Settings screen behavior — it is toolchain/catalog wiring only. Acceptance Criteria should be build/compile-green + dependency-resolution checks, not behavioral characterization tests.

## Research outcomes the planner must carry forward (2026-08-20)
- **`:app` is 100% Java** (zero `.kt`, no Kotlin plugin). Hilt there needs the Hilt Gradle plugin + `annotationProcessor hilt-android-compiler` and **no KSP/KAPT at all**; `annotationProcessor` is already the repo-wide idiom (**34 of the repo's 37 modules** — corrected in Plan Revision 2, D15; Research said "all 34"). `@HiltAndroidApp` goes on `PodcastApp.java:12`; **no `AndroidManifest.xml` change is needed**. The symbol-processing decision is real but attaches to `:ui:preferences` at Milestone 18 — recommendation is KSP, whose version must match `kotlin = 2.3.20` exactly.
- **The `kotlin-bom` 1.9.24 skew does not block this milestone** — measured, not assumed: `kotlin-stdlib` already resolves to 2.3.20; only the two content-free `-jdk7`/`-jdk8` shims sit at 1.9.24, and the BOM's stated KT-55297 rationale is obsolete. Leave / re-point / delete are all safe; none is a gate on starting.
- **`:app` needs nothing for Compose**, now or at Milestone 20. `:ui:preferences` is the right and only module. The Compose compiler plugin is already catalogued (`libs.plugins.kotlin.compose`) and already proven on this AGP/Kotlin combination by `app-wearos`.
- **Paparazzi is the only risky addition** — it shares a unit-test source set with Milestone 15's 38-test Robolectric regression net (and 15b's `@GraphicsMode(NATIVE)` bitmap render, if merged) — **superseded by Revision 3: 15b merged, so the net is 41 tests and the native-graphics render is unconditionally present.** Needs its own final step, a full both-flavour test re-run as AC, and a rollback criterion. Add-and-prove / add-unproven / defer-to-20 is an explicit decision the plan must make, not default.
- **An unused version-catalog entry is inert** — Gradle never resolves it. Any "confirm it resolves" AC requires a module to actually consume the entry.
- **Coroutines are already on `:ui:preferences` at 1.7.3** transitively (via `work-runtime` and `lifecycle-viewmodel-ktx`). Declaring them explicitly at a higher version is a real, disclosable change on a "no behavior change" milestone. `-rx3` is recommended deferred to Milestone 17.
- **The hidden cost is static analysis, not wiring**: `-Werror` javac, `lint { warningsAsErrors true, checkGeneratedSources = true, checkDependencies true }`, and SpotBugs over generated bytecode with a custom hook that throws on any finding and no dagger/hilt exclusion. Budget a contingency step.
- **CI is stricter than `:app:assembleDebug`**: `assemblePlayDebug` + `assemblePlayRelease` + `assembleFreeRelease` (two R8-minified), `assemblePlayDebugAndroidTest`, `checkstyle lint`, `ktlintCheck`. Acceptance Criteria should match that bar.
- **No new repositories are needed** — everything resolves from the already-declared `google()` / `mavenCentral()` / `gradlePluginPortal()`.
- **Version numbers were deliberately not guessed.** Hilt, KSP, Paparazzi, coroutines, and the Compose BOM all need resolving against the real repositories by the planner or developer, with the AGP 8.11 / Gradle 8.13 / JDK 21 / Kotlin 2.3.20 constraints recorded.

## Plan outcomes the red-team and developer must carry forward (2026-08-20)

Decisions are D1–D14 in the task file's `## Plan` → `### Resolved Decisions`. The load-bearing ones:

- **Version pins, re-resolved against the live repositories in Revision 1 on 2026-08-20 (not recalled, not carried over):** Hilt **2.60.1**, KSP **2.3.11**, coroutines **1.9.0**, Compose BOM **2026.06.01** *(Rev 1 — was 2025.08.01)*, Paparazzi **2.0.0-alpha05** *(Rev 1 — was deferred)*. Each is re-verified by the developer before it is written into the catalog (AC1); a yanked version is a finding, not a number to adjust. No metadata anywhere asserts Hilt-vs-AGP-8.11 or Paparazzi-vs-AGP-8.11 compatibility (both consume AGP `compileOnly` / declare no AGP dep), so those claims are established by building, at AC4.
- **⚠️ THE FINDING THAT OVERRODE JOSÉ'S LITERAL INSTRUCTION — Compose BOM `2026.08.00` is hard-incompatible with this repo (D4).** Its Compose UI 1.12.0 declares `minCompileSdk=37` and `minAndroidGradlePluginVersion=9.1.0` (read from `ui-android-1.12.0.aar` → `META-INF/com/android/build/gradle/aar-metadata.properties`); this repo is `compileSdk 36` / AGP **8.11.0**, and AGP enforces both. Taking it would mean a `compileSdk` bump **and** a major AGP upgrade across 34 modules — and per upstream would worsen the Paparazzi conflict below. **Chosen instead: BOM `2026.06.01`** (Compose UI **1.11.4**, **material3 1.4.0**), verified per-artifact as `minCompileSdk ≤ 35` / AGP `≥ 8.6.0`. Every BOM train from `2026.04.01` to `2026.08.00` carries the same material3 1.4.0, so the substance of the ask lands; only Compose UI differs. **New AC2b makes this check mandatory and forbids "fixing" a floor violation by raising `compileSdk` or AGP.**
- **`app-wearos` convergence attempted and measured unreachable (D4).** No Wear Compose release targets Compose UI 1.11.4: `compose-material3` **1.6.0** (current pin) → ui 1.9.0; **1.6.2** (latest stable) → still 1.9.0; **1.7.0-beta01** (latest of any kind) → 1.10.0. Forcing the BOM onto `app-wearos` was rejected because that module has **zero test files**. **Disclosed outcome: the repo ends on two Compose UI versions** — `:ui:preferences` 1.11.4, `app-wearos` 1.9.0 — which cannot collide (separate application module, no dependency path between them). AC6 proves `app-wearos` did not move. This is a standing upstream ceiling, not a temporary state.
- **Paparazzi is now IN SCOPE at `2.0.0-alpha05`, mitigated with `forkEvery` and proven by one snapshot test (D6, rewritten).** Re-verified 2026-08-20: `2.0.0-alpha05` is still the latest (no 2.0.0 stable, no alpha06; last publish 2026-05-20), and cashapp/paparazzi **#1979 is still open**, 20 comments, last activity 2026-08-18. **Revision 0's error was reading two of the five workarounds in that thread.** All five were assessed: package-move (**refuted upstream** — ordering-dependent), `robolectric.properties sdk=34` (best-evidenced but would drop M15's 38 tests from targetSdk 36 to 34 — kept as **rung 2**), JUnit categories + separate CI job (needs `checks.yml`, out of scope), separate Gradle module (**rung 3**), and **`forkEvery = 1` — CHOSEN**. Its only documented objection is cost across many modules; here it applies to one module with **8** test classes (Revision 3 — was 7 before 15b merged). **Rung 4** is Robolectric `4.16 → 4.16.1` (upstream reports 4.16 specifically exacerbates the bug), **15** Robolectric modules / **37**-module blast radius (corrected in Revision 2, D15), own commit.
- **The Paparazzi step is run as an experiment, and the failure case is recorded too (AC23).** Step 11 runs the suite **without** `forkEvery` first to observe the conflict, then applies the mitigation, recording both outcomes verbatim plus before/after wall-clock. Paparazzi tests are ordinary JUnit tests, so `testPlayDebugUnitTest` executes the new snapshot test in the same JVM as the **41** Robolectric tests (Revision 3), **3 of which already drive layoutlib's native path via `@GraphicsMode(NATIVE)`** — the milestone's own regression command *is* the experiment. Subject is `R.layout.authentication_dialog`, an existing View-system layout (**no Compose needed**), already covered by 4 Robolectric tests.
- **Stop condition (D6, tightened in Revision 2):** Paparazzi is sequenced **last** (Steps 10–12) precisely so it is expendable. If the ladder is exhausted, **one final commit reverts every commit made under Steps 10, 11 *and* 12** — including any rung artifacts Step 12 created (`robolectric.properties`, the `settings.gradle` entry plus its empty module, the Robolectric `4.16.1` pin), which Revision 1's "revert Steps 10–11" would have stranded. The evidence is reported to José and the milestone still ships. Steps 1–9 and 13 are unaffected. **AC25 governs the shipped diff in that case and states which criteria go void, change meaning, narrow, or re-anchor** — so an implementation reviewer reads an unmet AC23 as a triggered stop condition, not a failed step.
- **The one production-source change is verified by running the app, not by building it (Revision 2, D12/AC26/AC27).** `@HiltAndroidApp` ships in the real R8-minified APK; AC4 only proves R8 ran and AC9 only proves the Hilt component survives Robolectric's unshrunk JVM classpath. AC26 installs and launches at Step 3 and Step 13, on `installPlayDebug` **and** `installPlayRelease`, asserting on `pidof` + `logcat` (not `monkey`'s exit code — `PodcastApp.java:18`'s crash handler swallows failures into a log file `PreferenceUpgrader.java:36` may delete). `installPlayRelease` needs no new prerequisite: `app/build.gradle:39` signs `release` with the same throwaway `app/keystore` D12 already requires. AC27 captures `mapping.txt` + dex evidence that `Hilt_PodcastApp` survived shrinking, in addition to AC26 and never instead of it. **No device available ⇒ capture AC27, write the gap into Implementation Notes, escalate to José — never silently waive.**
- **Four corrections to Research, all measured, all consequential:**
  1. **This repo is not on Compose 1.7.5.** `app-wearos` resolves `compose.ui`/`compose.runtime` **1.9.0** ("By conflict resolution: between versions 1.9.0 and 1.7.5") — `wear-compose:1.6.0` requires 1.9.0 and the catalog's `compose = "1.7.5"` is a losing floor. Research's BOM option (a) (`2024.11.00`, ui 1.7.5) would have put `:ui:preferences` on a runtime this repo has never compiled. **Superseded by Revision 1:** the BOM is now `2026.06.01` (ui 1.11.4, material3 1.4.0) per José's OQ2 answer, which forfeits the "already-proven runtime" argument — that forfeit is disclosed in D4 and is the tradeoff he authorised.
  2. **KSP is no longer versioned `<kotlin>-<ksp>`.** KSP2 versions independently: `2.3.11`'s POM declares `kotlin-stdlib 2.3.20` and its Gradle plugin declares no KGP dependency at all. Research's "single hard version constraint" has dissolved.
  3. **The repo already runs two coroutines versions** (`:ui:preferences`/`:app` 1.7.3, `app-wearos` 1.9.0). Picking **1.9.0** makes the `kotlinx-coroutines-play-services` ref re-point a provable no-op and keeps the disclosed bump to 1.7.3 → 1.9.0.
  4. **`@HiltAndroidApp` executes inside `:app`'s existing Robolectric unit tests.** `app/src/test` has 4 classes, 2 Robolectric-driven (`FeedDiscovererTest`, `ShownotesCleanerTest`); Robolectric instantiates `PodcastApp` and calls `onCreate()` per test, which after the annotation builds the Hilt `SingletonComponent`. Research's "no `:app` test source set changes needed" is true about changes and misleading about risk. Own step, own criterion (AC9).
- **Coroutines and Compose are WIRED into `:ui:preferences`, not merely catalogued** — so the additions are actually exercised (Research gap #3) and so the resolution movement lands in a diff with zero source change, where breakage is attributable. KSP is catalogued and root-declared but **applied to no module**; its KGP pairing is proven by a probe that is reverted before commit (D7, AC10).
- **`kotlin-bom` stays at 1.9.24 (D3)** — leave, document, defer to its own micro-task (OQ3). Disclosed consequence: Hilt requires `kotlin-stdlib 2.3.21`, so `:app` moves 2.3.20 → 2.3.21 while the two empty shims stay at 1.9.24. Must appear by name in the dependency diff.
- **The equivalence proof is a before/after resolved-dependency diff** across six fixed `dependencies` invocations, captured in Step 1 and closed in **Step 13** (D13, AC5/AC6) — including `:app-wearos`, which must diff **empty**.
- **Repo counts are 37 modules / 15 Robolectric modules / 34 `annotationProcessor` modules / 35 other-module `build.gradle` files (D15, Revision 2).** Research's `34 modules` and `14 Robolectric modules` are superseded; `34` was the `annotationProcessor` count mislabelled "one per module." Counts are now re-measured like version pins, never carried forward.
- **Verification bar = CI's real matrix** (D12): `assemblePlayDebug` + `assemblePlayRelease` + `assembleFreeRelease` (2 of 3 R8-minified) + `assemblePlayDebugAndroidTest` + `checkstyle lint` + `ktlintCheck`, plus both flavoured `:ui:preferences` test tasks locally. **Release assembly needs a throwaway keystore** the developer must generate exactly as CI does — and `.gitignore`'s `*.keystore` does **not** match a file named `keystore`, so `app/keystore` and `app-wearos/keystore` must never be staged (AC13).
- **Static-analysis contingency is planned, not a surprise** (D10/D11): `dagger-lint-aar` arrives on `:app` into `lint { warningsAsErrors true, abortOnError true, checkGeneratedSources true, checkDependencies true }`, so Dagger's and Compose's lint checks newly run over `:app` and 20+ dependency modules. Remedy ladder is pre-decided and bounded; nothing on it weakens the repo's static-analysis posture or edits production source to satisfy a checker.
- **Base branch (D1, Revision 3):** fetch `origin`, confirm **both** `f5d4c5551` (M15/PR #21) **and** `45904a410` (M15b/PR #22) are ancestors of `origin/develop`, then branch from `origin/develop`. **15b is merged, so there is no sibling-branch coordination question at all** — the old "independent of PR #22 because D6 defers Paparazzi" reasoning is retired (Revision 1 reversed that premise; the merge settles the rest). If either ancestry check fails, stop: the baseline this plan's counts are written against is not the one in front of you.
- **Test baseline (Revision 3):** `:ui:preferences` `screen/synchronization/` on `origin/develop` = **11 files / 8 test classes / 41 `@Test` methods** (Gpodder 9, SyncPrefs 8, Nextcloud 6, HarnessSmoke 6, AuthDialog 4, Lifecycle 4, ScreenshotCapture 3, JavaInterop 1), plus 3 support files with no tests. **41** through Step 8, **42** from Step 11 (AC7). Every existing file is frozen; Step 11's Paparazzi test is a **twelfth**, new file.

## Resume command

**Next stage: `migration-code-reviewer`.** Implementation is complete, shipped without Paparazzi. Review against the Plan **as amended by Revisions 1–3**, with AC25's disposition table governing which criteria are VOID/CHANGED MEANING/NARROWED/RE-ANCHORED rather than failed. Five commits on the branch (`316481c2e`, `1e387562a`, `766aaf993`, `78f13f047`, `59287054f`), plus this stage's doc-only commit (README conventions 10–12, future-work file, task file Implementation Notes, this checkpoint). Full evidence, the D13 dependency diff, and every AC's disposition are in `tasks/antennapod-sync-settings-toolchain-milestone-16.md` → `## Implementation Notes`.

**Prior resume command, superseded, retained as history:**

> Implement `tasks/antennapod-sync-settings-toolchain-milestone-16.md` per its `## Plan`, as amended by `## Plan — Revision 3`. Thirteen Steps in order, strictly inside File Scope, one reviewable commit per step, build green at every step. Read the Plan **plus all three revision sections** before Step 1 — Revisions 1–3 amend decisions in place and the revision sections say what moved and why.

> Implement `tasks/antennapod-sync-settings-toolchain-milestone-16.md` per its `## Plan`, as amended by `## Plan — Revision 3`. Thirteen Steps in order, strictly inside File Scope, one reviewable commit per step, build green at every step. Read the Plan **plus all three revision sections** before Step 1 — Revisions 1–3 amend decisions in place and the revision sections say what moved and why.

**Do not re-invoke `legacy-android-planner` or `legacy-android-red-team` on this plan.** Loop 2 of 2 was final under the standing max-2-loop rule; José closed its one finding by authorising Revision 3 as a narrow out-of-cycle factual correction (not a new design surface, not a Loop 3). The next red-team pass is the **implementation** one, after code review.

**Standing instruction to the developer — verify the corrected baseline yourself.** Revision 3 exists precisely because a prior stage asserted a measured baseline that a fresh check contradicted, and the fix for that is not a more confident assertion. Do not take **11 files / 8 classes / 41 tests**, `origin/develop` tip `45904a410`, or any per-class row from this checkpoint or from the Plan on trust. At **Step 1**, before writing a line: re-run `git fetch origin`, re-confirm both ancestries, re-derive the per-class table from an actual `:ui:preferences:testFreeDebugUnitTest --rerun` / `:testPlayDebugUnitTest --rerun` run, and record **your** numbers as the frozen baseline in Implementation Notes. **Any divergence — including `origin/develop` having moved again since 2026-08-20 — is a finding to record and escalate, not a discrepancy to silently reconcile.** The same applies to every version pin (AC1) and every count in the Plan: this milestone's whole case-study angle is that its facts are measured rather than recalled, and you are the stage that measures them.

`tasks/antennapod-sync-settings-toolchain-milestone-16.md` holds, in order: `## Plan` (D1–D15, 13 Steps, File Scope, AC1–AC27 / 29 criteria, OQ1–OQ5), `## Plan — Revision 1`, `## Red-Team Verdict — Plan` (Loop 1, CHALLENGE), `## Plan — Revision 2`, `## Red-Team Verdict — Plan (Loop 2, Final)` (CHALLENGE), and **`## Plan — Revision 3`** (the baseline correction that closes it). The Plan's two convention notes at the top of `## Plan` state which figures govern: **37 modules / 15 Robolectric modules** (D15, Revision 2) and **11 files / 8 classes / 41 tests** (Revision 3).

**The Loop 2 blocker is closed.** It was: `origin/develop` moved after Research ran — Milestone 15b (PR #22) merged at `45904a410` on 2026-08-20 — making the real baseline 11 files / 41 tests rather than the Plan's 9 / 38, so AC7's literal totals (38 → 39) could not pass, and D1's "no coordination with PR #22 required" no longer followed from either its own premise or the repo state. Revision 3 re-verified all of that independently against the remote (the red-team's number was confirmed, not assumed), rebased AC7 to 41 → 42, rewrote D1, annotated D6, and swept fourteen other references. **Nothing else in Loop 2's review needed work** — see below.

**Two things carried forward for the developer, neither blocking:**
- **AC26 needs a device or emulator.** Loop 2 confirmed the plan handles a missing one correctly (capture AC27's static R8 evidence, write the gap into Implementation Notes, escalate to José — never silently waive). One MINOR open point the plan does not settle: whether "escalate to José" pauses the auto-chain or merely flags it prominently in the PR. **If AC26 turns out to be unrunnable, stop and ask rather than guessing** — that is the reasonable default for an explicitly-flagged human-decision point. `adb`/`emulator` binaries do exist in this sandbox (`~/Library/Android/sdk`) even though no device is currently attached, so AC26 is not obviously impossible here.
- **Step 13's README conventions are numbered from 10**, not 9 — 15b's merge brought convention 9 (Robolectric native-graphics capture) onto `develop`. The new `forkEvery` convention should cross-reference it rather than restate it.

**Loop 2's outcome on Loop 1's findings:** all four of Loop 1's findings were independently re-verified against the live repo (not taken on Revision 2's narrative) and confirmed genuinely closed — the counts (37/15/34/35, re-derived from scratch and matched), AC25's per-criterion disposition table (checked exhaustive: 28 of 29 criteria accounted for, AC25 itself excluded), AC26/AC27's citations (every line number and default value checked against `app/build.gradle` and `PodcastApp.java`/`PreferenceUpgrader.java` directly, all exact), and the AC2b Half-A/Half-B split. None of those need further work.

**Where the work stands:** no code has been written and no branch has been cut. The working tree is still on `test/ui-preferences-sync-settings-before-screenshot` (15b's own branch, now merged), and only this task file and its checkpoint are untracked. **Step 1 cuts the branch fresh from a fetched `origin/develop`**, which contains both Milestone 15 and Milestone 15b. `AGENTS.md` forbids committing on `develop`/`master`.
