# Checkpoint — antennapod-sync-settings-kotlin-milestone-15

> **Repo:** `antennapod`
> **Task file:** `tasks/antennapod-sync-settings-kotlin-milestone-15.md`
> **Future-work file:** `tasks/antennapod-sync-settings-modernization-future-work.md` (Milestones 16–20)

## Status
**PR OPENED: https://github.com/josegbel/antenna-pod-kt/pull/21 (`kotlin/ui-preferences-sync-settings` → `develop`).**

## Last updated
2026-08-06

## Lifecycle progress
- [x] Research (legacy-android-researcher) — surveyed the full vertical slice (4 Java files, 704 LOC, zero tests anywhere in `ui:preferences`). Corrected two carried-over facts: the app is Material3 not Material2 (tolerance-based visual-parity bar still holds, for a different reason — 6 wallpaper-derived theme permutations can't be byte-identical to any Compose `ColorScheme`), and it's RxJava3 not RxJava2. Found `AuthenticationDialog` is subclassed by 2 `:app` Java classes (not slice-private), the screen is settings-search-indexed (a `PreferenceFragmentCompat` type propagates to 9 `:app` call sites), Hilt cannot be confined to a library module, and there's no ViewModel/MVVM layer anywhere in this slice (blocking prerequisite for the eventual `compose` milestone). Enumerated 27 uncovered behaviors needing characterization tests before conversion. Full record in the task file's Research section.
- [x] Plan (legacy-android-planner) — 15 Decisions, 12 Steps, File Scope, 22 ACs, 5 Open Questions. `kotlin` track only. **Research Unknown 9 resolved and found worse than stated: local `origin/develop` is stale (predates PR #20's merge)** — D1 requires `git fetch origin` plus an explicit ancestry check before branching, with an explicit stop-and-ask if it still fails. Two corrections to Research: the "Robolectric-free precedent" is module-local to `:net:sync:service-interface`, not repo-wide (15 modules use Robolectric) — what's actually first-of-kind is that nothing in this repo drives a Fragment/Activity in a test, so Step 1 proves that harness before any behavior assertion; and CI never actually runs `testFreeDebugUnitTest` for flavoured modules (filed as OQ4). Zero version-catalog changes needed — only two plugin aliases + three `testImplementation` lines in `ui/preferences/build.gradle`. All 27 characterization gaps accounted for (26 executable via a `SynchronizationQueue.instance` recording fake with no DI/production change needed; gap 16 has no executable pin and is filed as OQ2). Sharpest decision (D11 rule 1): the nullable `fromIdentifier` `!!` must be forced at the assignment site, not the call sites, because forcing later would skip an observable `preferenceHeader.setTitle("")` — pinned by a live-falsification AC. `AuthenticationDialog` interop guarded three ways (assembleDebug, a Java oracle test, `javap -p`). Three known defects pinned not fixed, per README #11. Two items flagged for José, not an agent: OQ1 (whether Milestone 17's ViewModel/MVVM prerequisite is a funded line item or blocks Milestone 20) and OQ3 (recommend capturing the "before" screenshot manually at the end of *this* milestone rather than waiting for Milestone 20). Full record at `## Plan` / `## Open Questions` in the task file.
- [x] Red-team plan (legacy-android-red-team) — **Loop 1: CHALLENGE** (4 findings: CRITICAL unscoped grep on AC13 that would false-fail on an unrelated legitimate `error.getMessage()` call; MAJOR `service!!` undercount, 3 vs actual 4; MAJOR wrong line/parameter citation in D8/AC15's interop test justification; MINOR Robolectric module count off by one, 15 vs 14). Plan revised (Revision 1) fixing all four; fixing the `!!` count also required correcting two already-wrong downstream derived totals (37, not the stated 30). **Loop 2 (final): APPROVE.** All four Loop 1 fixes independently re-verified against source, including hand-summing D11's `!!` table (27+8+2+0=37, reconciles). One new MINOR item surfaced and resolved inline: two more possible `!!` sites in an `ArrayAdapter.getItem()` call (confirmed nullable via `javap` against the project's actual compileSdk 36 jar) — classified non-blocking since the adapter's backing data can never actually be null at that call site, and AC8's existing disclosure clause covers it if it surfaces at Step 11. No third loop needed. Full verdicts at `## Red-Team Verdict — Plan` in the task file.
- [x] Implement (android-migration-developer) — all 12 Steps complete. Branched fresh from `origin/develop` (tip `18f6b20ba`, PR #20 present, re-verified per D1 since the Plan's own evidence was already stale by implementation time). 38 characterization tests written Steps 1–7 before any production line changed; 4 files converted Java→Kotlin Steps 8–11 (`AuthenticationDialog`, `NextcloudAuthenticationFragment`, `GpodderAuthenticationFragment`, `SynchronizationPreferencesFragment`, in that leaf-first order per D10); Step 12 verification swept all 22 ACs and updated `ui/preferences/README.md` + the future-work file per D15. Both flavors 38/38 green throughout, `:app:assembleDebug` green throughout, module-scoped `ktlintCheck`/`checkstyle`/`lint` green throughout. **Major disclosed deviation:** the `!!` inventory is 49 operators, not the Plan's 37 — every additional site is either a genuine preserved Java `NullPointerException` the Plan's D11 table didn't enumerate (`Fragment.getContext()`/`getActivity()`/`getDialog()`, all unguarded in the original Java too) or a Kotlin null-checker requirement with no reachable Java crash behind it (reading a mutable `var` field in the same function it was just assigned in). Full reconciliation table and per-site justification in the task file's Implementation Notes. `GpodderAuthenticationFragment` had to become `open class` to keep the frozen test suite's one subclass compiling. Two `UseRequireInsteadOfGet` Android Lint findings resolved via targeted `@SuppressLint`, not the recommended `requireContext()`/`requireView()` (which the Plan's D11 rule 2 explicitly forbids, since they throw a different exception type). One build.gradle line beyond D4's stated three (`testImplementation project(':model')`, needed for `RecordingSynchronizationQueue`'s override signature). Full record in the task file's `## Implementation Notes` section.
- [x] Code review (migration-code-reviewer) — **Loop 1: REQUEST CHANGES** (MAJOR: ~30 lines of newly-added explanatory comments across the 4 converted production files, violating `AGENTS.md`'s strict no-added-comments rule; everything else, including an independent re-derivation of the 49-vs-37 `!!` reconciliation, checked out clean). Fixed same-session. **Loop 2: REQUEST CHANGES** (MAJOR: reviewer extended the same rule to the 8 new test files — 7 of 8 also had newly-written comments, ~60 lines, since the module had zero tests before this milestone so none of it carries over from an original to preserve). Fixed same-session. **Loop 3 (final): APPROVE**, no findings open — independently re-verified all comments gone from both waves, re-ran both flavors' tests (38/38), `:app:assembleDebug`, and `ktlintCheck` fresh rather than trusting the self-reports. Full verdicts at three `## Code Review Verdict` sections in the task file.
- [x] Red-team implementation (legacy-android-red-team) — **APPROVE** (loop 1 of max 2, final). Independently re-derived the 49-vs-37 `!!` reconciliation from scratch (own `grep`, own line-by-line read of both largest Java originals), confirmed all three pinned defects (RxJava `Disposable` leak, `devices` data race, recycled-ViewHolder-as-field) are preserved not fixed, confirmed `AuthenticationDialog`'s Java interop surface against source/bytecode/a live `:app:assembleDebug`, re-ran `testPlayDebugUnitTest` itself (38/38), and spot-checked four characterization tests' actual assertions against the Java source they claim to pin. One MINOR concern (non-blocking): the `chooseProviderAndLogin` adapter's recycled-holder cast changed from Java's null-tolerant `(ViewHolder) convertView.getTag()` to Kotlin's `as ViewHolder`, which throws `ClassCastException` instead of passing `null` through if `view.tag` were ever null — carries no practical risk given the adapter's closed usage (it only ever receives views it created itself), flagged as a documentation nicety for whichever milestone next touches this file. Full verdict at `## Red-Team Verdict — Implementation` in the task file.
- [x] PR opened — https://github.com/josegbel/antenna-pod-kt/pull/21

## Decisions for next session
- **Scope narrowed 2026-08-06, after Research, before Plan.** Originally scaffolded as one milestone covering `kotlin` + `di` + `concurrency` + `compose` together (file was named `antennapod-sync-settings-modernization-milestone-15.md`). Research's findings (Hilt can't be confined to a library module; no ViewModel exists, blocking `compose`; `:storage:preferences` is a 5-module blast radius; zero test coverage anywhere) made the case that this was too large for one focused PR. José agreed to split it, matching this portfolio's established one-track-per-milestone pattern (see `tasks/antennapod-model-kotlin-future-work.md` for precedent). **Task file and this checkpoint renamed to `antennapod-sync-settings-kotlin-milestone-15`; Milestone 15 is now `kotlin` track only.** Remaining tracks sequenced as Milestones 16 (toolchain/infra) → 17 (`concurrency` + MVVM) → 18 (`di` wiring) → 19 (`:storage:preferences`, scope TBD) → 20 (`compose`), recorded in `tasks/antennapod-sync-settings-modernization-future-work.md`.
- Module/slice: Sync Settings screen, `ui:preferences/screen/synchronization/` (4 Java files: `SynchronizationPreferencesFragment`, `GpodderAuthenticationFragment`, `NextcloudAuthenticationFragment`, `AuthenticationDialog`). Milestone 15's own scope: convert all 4 to Kotlin, stand up `ui:preferences`'s test source set from nothing (none exists today), write characterization tests for the 27 behaviors Research enumerated. `:storage:preferences` stays Java for this milestone (calling Java from Kotlin is not a blocker).
- PR #20 (Milestone 14, `:net:sync:service-interface` test suite → Kotlin) merged into `develop` 2026-08-06. Builds on `:net:sync:service-interface` (Kotlin) and `:event` (Kotlin), both already migrated.
- **Housekeeping flagged by Research (Unknown 9): the working tree is currently on branch `antennapod-net-sync-service-interface-kotlin-milestone-14`, not `develop`.** Branch fresh from `origin/develop` (which contains merged PR #20) before the first implementation commit — AGENTS.md forbids committing directly on `develop`/`master`.
- Locked decisions (visual-parity tolerance bar, `ComposeView`-with-real-Compose-content hosting) apply to the eventual Milestone 20 (`compose`), not to this milestone — recorded in the task file's pre-research context and unaffected by the Material2→Material3 correction.
- José gave the go-ahead to scaffold the original milestone on 2026-08-06, directly after Milestone 14's PR merged, and agreed to the 6-milestone split the same day after reviewing Research.

## Resume command
Milestone 15 is complete end to end: implementation (12/12 Steps), code review (3 loops, APPROVE), red-team implementation review (APPROVE), and PR #21 (https://github.com/josegbel/antenna-pod-kt/pull/21) opened against `develop`. Next milestone in the sequence is Milestone 16 (toolchain/infra), per `tasks/antennapod-sync-settings-modernization-future-work.md` — do not start it until PR #21 is reviewed/merged, matching this portfolio's established one-PR-in-flight-at-a-time pattern.
