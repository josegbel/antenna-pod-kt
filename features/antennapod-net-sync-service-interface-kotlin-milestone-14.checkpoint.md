# Checkpoint — antennapod-net-sync-service-interface-kotlin-milestone-14

> **Repo:** `antennapod`
> **Task file:** `tasks/antennapod-net-sync-service-interface-kotlin-milestone-14.md`

## Status
**IN PROGRESS — Code review loop 1 finding fixed, awaiting loop 2**

## Last updated
2026-08-05

## Lifecycle progress
- [x] Research (legacy-android-researcher) — 9 Java test files / 1,179 LOC / 83 tests / 162 assertions confirmed against source, green on both variants at HEAD; zero hard keywords and zero numeric-widening traps, but ≈28 `!!` sites (all in `EpisodeActionJsonTest`) and a README convention #9 that says this suite must stay Java — measured file-by-file, only `ISyncServiceTest` loses both guards, and nothing ends with zero residual coverage.
- [x] Plan (legacy-android-planner) — 17 Decisions, 7 Steps, 25 ACs. **Convert 8 of 9; `ISyncServiceTest.java` stays Java** (D2: strip its compile-time oracle and all four of its tests reduce to a stub declared inside the file asserting itself — the `:event` M9 shape, not the M13 shape; the three other README #9 files keep guards that survive, strengthen, or trade up to reflection). The 29 `!!` are inventoried exactly, with the 23 nullable-*return* sites forced at the **assignment** site rather than at an assertion (D4, scope narrowed in Revision 1), and the three vacuity sites are proven load-bearing by live falsification — 3 tests × 2 forms, `!!` must FAIL and `?.` must PASS under a null-producing fixture (D5, AC10). Conventions #6a/#6b lose their in-module guard and are re-proven by `javap` (D6), which also re-runs M11 D9's `DateFormat.format` binding check. README convention #9 rewritten (D3); test tasks corrected to unflavoured `testDebugUnitTest`/`testReleaseUnitTest` throughout.
- [x] Red-team plan (legacy-android-red-team) — **Loop 1: CHALLENGE.** D2's stub-asserting-itself analysis, D5's falsification procedure, and the corrected unflavoured task names all independently re-verified and confirmed correct. Three findings, none touching File Scope/Steps/Decisions substance: **MAJOR** — AC11's `grep -rc '!!'` command undercounts `EpisodeActionJsonTest.kt` by 1 (line 414 carries two `!!` on one physical line; `grep -c` counts matching lines, not occurrences, so a fully correct 29-`!!` implementation reports 27, not the stated 28 — will misfire on correct code and invites an unforced diff "fix"). **MINOR** — D4's "forced at the assignment site, never at the assertion" is contradicted by its own table for 6 of 29 sites (5 `timestamp!!.time` + 1 `cause!!.message`, forced inline in `assertEquals`/`assertNotEquals` args); verified this does not reintroduce Gap 1's vacuity risk (none are `assertNull`), so it's a prose-precision fix only. **MINOR** — the `:event` M9 `PublicFieldInteropTest` analogy in D2 is imprecise: that file's kept-Java rationale was "real production behavior, but redundant with other converting tests," not "a stub asserting itself" — same rule, different mechanism; D2's own conclusion for `ISyncServiceTest` holds independently of the analogy. Full verdict at `## Red-Team Verdict — Plan` in the task file.
- [x] Plan revision (legacy-android-planner) — **Revision 1: all three findings accepted in full**, none taken on the red-team's word. MAJOR: AC11's command replaced with a per-file `grep -o '!!' | wc -l` occurrence count; I re-swept the file for *any* doubled site rather than accepting that `:414` was the only one (it is — the 15 read and 8 write forcings are each on their own statement line), so the corrected figures are 28 occurrences / 27 lines in `EpisodeActionJsonTest.kt` and 29 / 28 suite-wide, and restructuring `:414` to satisfy a counting tool is now explicitly forbidden. MINOR: D4's heading and body narrowed to scope the assignment-site rule to the 23 nullable-return sites, with a new paragraph justifying the other 6 affirmatively (no local exists to force at; all are `assertEquals`/`assertNotEquals`, so a `?.` softening fails visibly — which is also why D5 targets the three `assertNull` sites and no others). MINOR: D2's `PublicFieldInteropTest` citation rewritten as same-rule/different-and-starker-mechanism (redundant-but-real vs. self-referential stub) — this correction *strengthens* D2's conclusion while weakening my original support for it. Three downstream repetitions of the overstated claim found and fixed (Step 5, D15, and — the one that mattered — **AC12**, whose opening clause "No `!!` sits at an assertion site" would have contradicted the corrected D4 and could have failed a correct implementation on prose alone; its two grep commands were always correctly scoped and are unchanged). Counts unchanged: 17 Decisions, 7 Steps, 25 ACs, 8 renamed + 1 held back. Full record at `## Plan — Revision 1` in the task file.
- [x] Red-team plan loop 2 (legacy-android-red-team) — **Loop 2 (final): APPROVE.** All three loop-1 findings independently re-verified as correctly and consistently fixed, not just patched locally: AC11's `grep -o '!!' | wc -l` script genuinely counts occurrences (confirmed against a synthetic fixture) and is non-recursive/naturally excludes `ISyncServiceTest.java`; re-derived the 28-occurrence/27-line (in-file) and 29/28 (suite-wide) figures independently from my own loop-1 read of the file, confirming line 414 is the only doubled site; D4's affirmative justification for the 6 inline-forced sites re-checked directly against `EpisodeActionJsonTest.java`/`SyncServiceExceptionTest.java` — all 6 are `assertEquals`/`assertNotEquals` against non-null literals, never `assertNull`, so no vacuity risk; AC12/Step 5/D15 all read consistently with the corrected D4, and a full-file sweep for the three retired phrases ("never at the assertion" as a blanket claim, "exactly the shape", `grep -rc '!!'`) found no live occurrence outside the loop-1 verdict/Revision-1 narrative (i.e. no fourth stale reference). Decision/Step/AC counts (17/7/25) and File Scope (8 renamed, 1 held back) reconfirmed unchanged. No concerns raised. Full verdict at `## Red-Team Verdict — Plan (Loop 2, Final)` in the task file. **Plan is approved for implementation.**
- [x] Implement (android-migration-developer) — All 7 Steps complete on branch `antennapod-net-sync-service-interface-kotlin-milestone-14` (5 commits: Steps 2, 3, 4, 5, and a combined Step 6/7). Eight files converted Java→Kotlin (`SubscriptionChangesTest`, `EpisodeActionChangesTest`, `SynchronizationProviderTest`, `SyncServiceExceptionTest`, `UploadChangesResponseTest`, `SynchronizationQueueTest`, `EpisodeActionCharacterizationTest`, `EpisodeActionJsonTest`); `ISyncServiceTest.java` held back byte-identical to the merge-base (D2, AC6 confirmed empty diff). 83/83 tests green on both `testDebugUnitTest`/`testReleaseUnitTest`, two separate `--rerun` invocations, at every step and in Step 6's final reconciliation. Exactly 29 `!!` (28 in `EpisodeActionJsonTest.kt`, 1 in `SyncServiceExceptionTest.kt`), verified by occurrence count (AC11). **D5's live falsification actually run, not just reasoned about**: for the three `assertNull(readFromJsonObject(...).X)` sites, mutated the fixture to omit `"podcast"`, ran each test in both the shipped `!!` form (FAILED with `NullPointerException`, all three) and the softened `?.` form (PASSED vacuously, all three) — 6 recorded outcomes, then reverted; `EpisodeActionJsonTest` re-ran clean at 28/28. D10's per-file assertion-content diff empty for all 8 converted files, both per-step and in Step 6's consolidated sweep. `javap` re-proof confirms README #6a/#6b's lost in-module guards on the outer class (not just `$Companion`) and re-confirms Milestone 11's `DateFormat.format` overload-resolution fork on untouched production bytecode (`SimpleDateFormat.format(Date)`, matching M11's own historical record exactly). `net/sync/service-interface/README.md` convention #9 rewritten per D3 (four required statements, no milestone/task reference); `tasks/antennapod-model-kotlin-future-work.md` item 3 gains one paragraph, no new item. `git diff` against the merge-base for `src/main/` is empty; `:app:assembleDebug` is green. One disclosed naming clarification (not a substantive deviation): the module's genuinely-gating ktlint task is the unflavoured `ktlintTestSourceSetCheck`, not the buildType-suffixed `ktlintTestDebugSourceSetCheck`/`ktlintTestReleaseSourceSetCheck` AC7/AC22 name — this module has no product-flavor dimension so those buildType-suffixed tasks stay permanently `NO-SOURCE`/`SKIPPED` regardless of source-set content; the substantive requirement (ktlint gates the 8 Kotlin files) is fully met. Full record at `## Implementation Notes` in the task file.
- [~] Code review (migration-code-reviewer) — **Loop 1: REQUEST CHANGES.** Independently re-verified essentially every load-bearing claim in Implementation Notes by direct re-execution rather than trusting the self-report: both test variants re-run as separate `--rerun` invocations (83/83 each, per-class rows matching exactly), `ISyncServiceTest.java` confirmed byte-identical to the merge-base, D10's assertion-content diff re-run across **all 8** converted files (not a sample) with zero residual on every file, the `!!` inventory and D4's 23-assignment/6-inline split independently recomputed and matching exactly, D5's live falsification **reproduced end-to-end** (mutated the fixture, confirmed the shipped `!!` form FAILS with `NullPointerException` and the softened `?.` form PASSES vacuously, then reverted cleanly), all seven `javap -p`/`-c` rows reproduced against a fresh build matching the interop re-proof exactly, `:app:assembleDebug` BUILD SUCCESSFUL, `checkstyle lint` reproducing exactly the three pre-existing module-unrelated failures claimed, and the disclosed `ktlintTestSourceSetCheck` naming clarification confirmed accurate and genuinely inert (not masking a real gate) via `--dry-run` and a real run. 24 of 25 ACs confirmed exactly as claimed. **One MAJOR finding:** AC15's literal command (`grep -rnF '`' .../src/test/`) returns 14 hits, not the zero the Implementation Notes report as "PASS" — all 14 are benign KDoc Markdown-formatting backticks in doc comments (zero backtick-escaped Kotlin identifiers, so D14's actual concern is genuinely satisfied), but the self-report doesn't reproduce and wasn't disclosed the way the AC7/AC22 ktlint-naming gap was. Same-loop fixable: either strip the doc-comment backticks or disclose the gap with an accurate count, matching the AC7/AC22 disclosure precedent. Full verdict at `## Code Review Verdict` in the task file.
  - **Fix applied (android-migration-developer, same session):** re-read AC15/D14 to judge the right fix rather than defaulting to disclosure — D14's actual concern is a backtick-*escaped identifier* (zero legitimate cases per Research), and all 14 hits were cosmetic KDoc Markdown code-formatting backticks with no functional purpose in this non-doc-generating codebase, so stripping them (rather than carrying a permanent disclosed exception, the AC7/AC22 shape) is the fix more consistent with the Plan's intent here. Stripped the backticks from all 14 sites across the 5 named files (comment-only edits). Re-verified: `grep -rnF '`' net/sync/service-interface/src/test/` → 0 hits; D10's diff re-run for all 8 converted files — still empty; `!!` count unchanged at 29 (28+1); `ktlintCheck` BUILD SUCCESSFUL; both variants re-run 83/0/0/0 each. Implementation Notes' AC15 line and Deviations section updated with a "Post-review correction" subsection recording the fix and its reasoning. Not yet re-verified by `migration-code-reviewer` — **awaiting loop 2.**
- [ ] Red-team implementation (legacy-android-red-team)
- [ ] PR opened

## Decisions for next session
- Module: `:net:sync:service-interface`. Track: `kotlin` only, test-only conversion (9 Java test files, production already 100% Kotlin as of Milestone 11 / PR #17). Milestone 14. This is the suite the Milestone 13 checkpoint left explicitly queued.
- PR #19 (Milestone 13) is merged into `develop`. This milestone should branch from a fresh `origin/develop` that includes it.
- This is unaffiliated OSS portfolio work — see `services/android-migration/projects/portfolio/README.md`.
- José gave explicit go-ahead to start this milestone on 2026-08-05.
- **Correction from research:** this module does **not** apply `playFlavor.gradle`. Its only unit-test tasks are `testDebugUnitTest` / `testReleaseUnitTest` — `testFreeDebugUnitTest` / `testPlayDebugUnitTest` do not exist here. The task-file header's task names were carried over from Milestone 13's sibling module (which *is* flavoured) and are wrong for this one. Module README convention #8 already documented this. Separate `./gradlew` invocations per variant with `--rerun` still applies.
- Research left five Unknowns for the planner; Unknown 1 (convert all nine files, or hold `ISyncServiceTest` back under the inherited Java-oracle bright-line rule) is the milestone's central scoping decision and needs a written choice, not a default. **Resolved in the Plan as D2: hold `ISyncServiceTest` back.** Unknowns 2–5 resolved as D3 (README #9 rewritten in the same PR), D5 + AC10/AC11/AC12 (the `?.` vacuity hazard gets both a structural decision and a live-falsification acceptance criterion), D7 (callable references kept, lambda as the single disclosed fallback) and D6/AC20 (the M11 D9 `javap -c` binding check is re-run).
- **Found at planning time, not in Research:** a 29th `!!` site at `SyncServiceExceptionTest:83` (`Throwable.cause` is `Throwable?` in Kotlin) — Research's inventory said all `!!` were in `EpisodeActionJsonTest`; and an `Int`→`Long` compile trap at `ISyncServiceTest:38,49` (`UploadChangesResponse(list.size() + list.size())` against a `Long` constructor parameter), which is moot under D2 but recorded for anyone revisiting that decision.
- No feature branch was created — research ran read-only on a clean `develop` at `5f816b768`.
- **2026-08-05, live instruction:** José told the orchestrating (top-level) session directly, in conversation, to drive this milestone's pipeline through to an opened PR without pausing for a go-ahead between stages, matching Milestone 12 and Milestone 13's precedent. Each stage still applies its own full independent verification standard.

## Resume command
Code review loop 1's one MAJOR finding (AC15's backtick grep not reproducing as reported) is fixed on branch `antennapod-net-sync-service-interface-kotlin-milestone-14` — backticks stripped from the 14 KDoc sites, `grep -rnF '`' net/sync/service-interface/src/test/` now genuinely returns 0, D10's diff and the `!!` count reconfirmed unchanged across all 8 files, both test variants re-run 83/0/0/0. Implementation Notes' AC15 line and Deviations section updated with the correction and its reasoning (`## Implementation Notes` → "Post-review correction" in `tasks/antennapod-net-sync-service-interface-kotlin-milestone-14.md`). Not yet committed to git as of this checkpoint update — commit the fix (comment-only changes to the 5 named `.kt` files + this task file + this checkpoint file) before re-invoking. Next step: re-invoke **`migration-code-reviewer`** for loop 2 of max 3. Once code review approves: `legacy-android-red-team` for the implementation review, then open the PR — title/body should follow this milestone's summarized commit message (in Implementation Notes) and the repo's PR template at `.github/pull_request_template.md`, per `AGENTS.md`.
