# Checkpoint — antennapod-model-kotlin-milestone-3

> **Repo:** `services/android-migration/projects/portfolio/antennapod`
> **Task file:** `tasks/antennapod-model-kotlin-milestone-3.md`

## Status
**Red-team implementation APPROVED (loop 1 of max 2) — ready for PR**

## Last updated
2026-07-22

## Lifecycle progress
- [x] Research (legacy-android-researcher)
- [x] Plan (legacy-android-planner) — **revised in loop 2** to address all 4 red-team loop-1 findings (2 MAJOR + 2 MINOR).
- [x] Red-team plan loop 1 (legacy-android-red-team) — **CHALLENGE.** 2 MAJOR + 2 MINOR concerns, all now fixed in the Plan.
- [x] Red-team plan loop 2 (legacy-android-red-team) — **APPROVE (final loop).** All 4 loop-1 fixes independently re-verified against current real source; no new issues found. See `## Red-Team Verdict — Plan` (loop 2 entry) in the task file.
- [x] Implement (android-migration-developer) — all 7 Steps implemented exactly as planned, build green after every step, no deviations. See `## Implementation Notes` in the task file.
- [x] Code review (migration-code-reviewer) — **loop 1: REQUEST CHANGES** (1 MAJOR, real ktlint `standard:enum-wrapping` failure), **loop 2: APPROVE.** Loop 2 independently re-verified the fix against live source (not self-report): read current `MediaType.kt`/`SortOrder.kt` line-by-line against `FeedOrder.kt`'s precedent, forced a non-cached `./gradlew :model:ktlintMainSourceSetCheck --rerun` (genuinely executed, not `UP-TO-DATE`, PASS), confirmed via `ls -laT` mtimes that only the two enum files were touched, re-ran targeted + full `:model:test` and `:app:assembleDebug` (all green), and cross-checked the fresh `MediaTypeTest` JUnit XML directly (8/8 pass). See `## Code Review Verdict` (loop 2 entry) in the task file.
- [x] Red-team implementation (legacy-android-red-team) — **APPROVE (loop 1 of max 2).** No CRITICAL/MAJOR findings. Independently read all four `.kt` files against `git show HEAD`'s Java originals (not just code review's checklist), confirmed both named idiom traps (M2's `String.split()` trap, `serialVersionUID`/`Serializable` exposure) genuinely don't apply to this batch, sampled real downstream call sites for `SortOrder`/`MediaType` including a nested-enum Java cross-reference (`Feed.java:462` → `SortOrder.Scope.INTRA_FEED`), confirmed `isNullOrEmpty()` (not `isNullOrBlank()`) was used and its stdlib contract legitimately backs the smart-casts, confirmed `MediaMetadataRetrieverCompat.close()`'s override is a logic-for-logic match, and independently re-ran the full test suite myself (8/8 `MediaTypeTest`, 11/11 `SortOrderTest`, 14/14 `VolumeAdaptionSettingTest`, forced non-cached). One MINOR, non-blocking: the task file has repeated a stale "13 tests" count for `VolumeAdaptionSettingTest` since Research (actual count is 14) — zero equivalence impact, cosmetic only. See `## Red-Team Verdict — Implementation` in the task file.
- [ ] PR opened

## Decisions for next session
- This is Milestone 3 of the `:model` `kotlin`-track case study. Milestone 1 (8 framework-free enums/POJOs) and Milestone 2 (`FeedFilter`, `DownloadResult`, `FeedItemFilter`, `Transcript`) are both merged into `develop` on `josegbel/antenna-pod-kt` (PRs #1–#4), plus a follow-up README clarification (PR #5).
- `:model` is now 15 Java / 12 Kotlin. Per Milestone 2's checkpoint, the remaining Java files split into these carried-forward tiers (re-verify against current source, don't assume the old triage still holds — Milestone 2's own research had to correct Milestone 1's carry-forward list):
  - Framework-coupled (needs Robolectric/instrumented tests): `MediaType`, `SortOrder`, `VolumeAdaptionSetting`, `MediaMetadataRetrieverCompat`.
  - Hand-written `equals`/`hashCode` (must convert to plain `class`, preserve equality verbatim): `Chapter`, `EmbeddedChapterImage`, `FeedFunding`, `Feed`, `FeedItem`, `FeedMedia`, `RemoteMedia`, `DownloadRequest`.
  - `Parcelable` marshalling (requires Robolectric/instrumented round-trip tests): `DownloadRequest`, `FeedMedia`, `RemoteMedia`, `Playable` interface.
  - Deferred from Milestone 2 specifically: `SubscriptionsFilter` (framework-coupled via `TextUtils`, zero test coverage anywhere) and `FeedPreferences` (four nested int-code/`fromCode` enums structurally like Milestone 1's picks, but bolted onto a 324-LOC class with 33 downstream consumers and only 2 downstream test references).
- Milestone 2's implementation surfaced two real J2K idiom traps worth re-checking for in this milestone's candidate files: (1) Kotlin's `String.split()` not stripping trailing empty tokens the way Java's does — bit `FeedItemFilter`; (2) `serialVersionUID` must be preserved on any class with a live `Serializable`/`Bundle.putSerializable` exposure, not just ones the Plan explicitly names (missed for `FeedItemFilter` until implementation red-team loop 1 caught it).
- Test-suite sequencing decision (confirmed by José in Milestone 2): characterization tests stay in Java, unchanged across each production-code conversion, until all 27 `:model` files are Kotlin — do not fold test-file Kotlin conversion into this milestone.
- API-preservation pattern (`@JvmField`/`@JvmStatic`/`const val`, no `data class` where reference or field-selective equality is relied upon) is documented in `model/README.md` and should carry forward as the default unless the researcher/planner finds a concrete reason to deviate.
- Working tree was confirmed clean before this session started (a stray uncommitted `README.md` revert of PR #5's case-study note was discarded via `git checkout -- README.md`).

## Decisions from research (2026-07-21)
- Inventory re-verified against source: still 15 Java / 12 Kotlin (27 total), no `androidTest` source set in `:model`. Carried-forward tiers all confirmed accurate.
- **Recommended M3 batch = the framework-coupled tier, 4 files:** `MediaType`, `SortOrder`, `VolumeAdaptionSetting`, `MediaMetadataRetrieverCompat` (mirrors M2's 4-file scope; 190 LOC total; all enum/tiny-class shaped).
- **Explicit exclusions:** `SubscriptionsFilter` (framework-coupled via `TextUtils.split/join`, ZERO tests anywhere — same trailing-empty-token trap as M2's `String.split`, no net) and `FeedPreferences` (324-LOC `Serializable` class; its 4 nested int-code enums cannot be carved out without either converting the whole Serializable parent or changing the `FeedPreferences.X` reference path for 35 consumers = bespoke restructuring). Neither belongs in this batch.
- None of the 4 batch candidates are themselves `Serializable`/`Parcelable`, so the `serialVersionUID` trap does NOT apply to this batch (it does apply to `FeedPreferences`, hence its exclusion).
- ~~Key open Unknown for the planner: Robolectric-in-`:model`~~ **RESOLVED by José 2026-07-21: no Robolectric.** José flagged an architectural objection during scoping — `:model` is meant to stay a plain data module, and adding an `android.*`-dependent test framework to characterize `TextUtils.isEmpty` would shim around framework coupling that would also block a future Kotlin Multiplatform iOS target (Robolectric itself is test-only and would never ship, but its necessity would be evidence of real main-source Android coupling in `commonMain`). **Decision: `MediaType`/`SortOrder` replace `TextUtils.isEmpty(x)` with Kotlin's `x.isNullOrEmpty()` as part of the J2K conversion** — identical null-or-empty semantics, zero Android dependency, no Robolectric needed. Full batch of 4 stays in scope. `AudioEffect`/`MediaMetadataRetriever` coupling in `VolumeAdaptionSetting`/`MediaMetadataRetrieverCompat` has no stdlib substitute and is explicitly out of scope for KMP-readiness work in this milestone — flagged for a future milestone if KMP is pursued. Task file's Research section (Summary, Constraints & Risks, Unknowns 1/3/4) updated accordingly.
- **Remaining open item for the planner:** Unknown 2 only — how to handle `MediaMetadataRetrieverCompat`'s JVM-untestable `close()` (no-unit-test exception vs. first instrumented test vs. defer the file).

## Decisions from planning (2026-07-21)
- Plan section of `tasks/antennapod-model-kotlin-milestone-3.md` fully populated (Objective, Resolved Decisions, 7 Steps, File Scope, Acceptance Criteria, Milestone, Out of Scope) plus 2 non-blocking Open Questions.
- **Unknown 2 RESOLVED:** documented no-unit-test exception for `MediaMetadataRetrieverCompat.close()` (framework-pure; no `androidTest` infra added; recorded in `model/README.md` in Step 7).
- **Unknown 4 RESOLVED:** `MediaType.fromMimeType` param → explicit `String?`; `SortOrder.fromCodeString`/`toCodeString` stay `String?`/`SortOrder?` — not left to J2K inference.
- **New planner call (flagged, non-blocking Open Question):** because `TextUtils.isEmpty` is the first executed line of `fromMimeType`/`fromCodeString`, the pre-conversion Java is un-executable on `:model`'s bare-JVM runner; so Steps 1 & 3 decouple the guard to pure-Java `x == null || x.isEmpty()` *in the same commit as the characterization tests* (one commit before the `.kt` rename) so "tests green before conversion" is met without Robolectric. Kotlin end-state still uses `isNullOrEmpty()`. José can collapse this into a single conversion commit if preferred.
- 7 ordered steps, each build-green: (1) MediaTypeTest.java + MediaType.java guard decouple; (2) MediaType.kt; (3) SortOrderTest.java + SortOrder.java guard decouple; (4) SortOrder.kt; (5) VolumeAdaptionSetting.kt (existing test unchanged, no adjustment needed); (6) MediaMetadataRetrieverCompat.kt; (7) model/README.md.
- `model/build.gradle` and `VolumeAdaptionSettingTest.java` explicitly NOT in File Scope.

## Decisions from red-team review, loop 1 (2026-07-21)
- **Verdict: CHALLENGE.** Full findings recorded in `## Red-Team Verdict — Plan` in the task file. Summary of what the planner must fix before loop 2:
  1. **MAJOR — `VolumeAdaptionSetting.value`/`adaptionFactor` API widening.** Step 5 + Acceptance Criteria say these become `@JvmField val`, but they are `private` in the current Java (`VolumeAdaptionSetting.java:17-18`), unlike `SortOrder.code`/`scope` which genuinely are `public final`. Making them `@JvmField` adds new public Java-visible API that never existed and that zero downstream callers use (verified by grep across all 15 `VolumeAdaptionSetting` references — all go through `toInteger()`/`getAdaptionFactor()`). Fix: `value`/`adaptionFactor` should be plain `private val`, not `@JvmField`.
  2. **MAJOR — `MediaMetadataRetrieverCompat.kt`'s `close()` missing `override`.** Verified via `javap` against the project's actual `compileSdk 36` android.jar that `MediaMetadataRetriever implements AutoCloseable` and declares a real, non-final `public void close() throws IOException`. Step 6's Kotlin snippet omits `override`, which is a hard Kotlin compile error (not a warning, unlike Java). Fix: Step 6 must read `override fun close() { ... }`.
  3. **MINOR — no characterization test for `SortOrder.fromCodeString`'s non-numeric-string branch** (`NumberFormatException` path). Optional to add `fromCodeStringNonNumericThrowsNumberFormatException` to `SortOrderTest.java`.
  4. **MINOR — `SortOrder.parseWithDefault`'s nullability not explicitly pinned** in Unknown 4's resolution (only `fromMimeType`/`fromCodeString`/`toCodeString` are addressed). Sole caller (`UserPreferences.java:829`) passes non-null both params, so low risk, but should be stated explicitly per the plan's own principle.
- Verified as correct and NOT flagged (no re-litigation needed in loop 2): the `TextUtils.isEmpty` → `x == null || x.isEmpty()` identity (checked against Android's actual javadoc — exact match); the guard-decouple two-commit sequencing necessity (bare-JVM "not mocked" issue is real, split is required, not just stylistic); `MediaType`/`SortOrder`'s 3-method nullability decisions (cross-checked against real call sites); `VolumeAdaptionSettingTest.java` needing no adjustment (signature match confirmed); File Scope correctness (`model/build.gradle`, `VolumeAdaptionSettingTest.java` correctly untouched); no scope creep into other tracks or architecture work.

## Decisions from planning loop 2 (2026-07-21) — red-team fixes applied
- **MAJOR 1 FIXED — `VolumeAdaptionSetting.value`/`adaptionFactor` visibility.** Confirmed against source (`VolumeAdaptionSetting.java:16-17`): both are `private` today. Step 5, the API-preservation Resolved Decision, and the Acceptance Criteria now specify plain `private val` (NO `@JvmField`), with `toInteger()`/`getAdaptionFactor()` as the sole Java-visible accessors — no API widening. `SortOrder.code`/`scope` correctly stay `@JvmField val` (genuinely `public final` today, `SortOrder.java:37,40`).
- **MAJOR 2 FIXED — `MediaMetadataRetrieverCompat.close()` `override`.** Step 6's snippet now reads `override fun close() { ... }`, and the Unknown 2 Resolved Decision documents both why `override` is mandatory (Kotlin, unlike Java, hard-errors on a missing override of `AutoCloseable.close()`) and that `throws IOException` needs NO Kotlin-side handling (no checked exceptions → no `@Throws`, no signature change; the exception is swallowed internally). New Acceptance Criteria line gates the override compiling.
- **MINOR 3 FIXED — non-numeric `fromCodeString` coverage.** Added `fromCodeStringNonNumericThrowsNumberFormatException` (`"not_a_number"`) to Step 3's `SortOrderTest.java` (now 11 methods). Confirmed against control flow (`SortOrder.java:64`): `Integer.parseInt` runs before the unknown-code loop, so the propagating type is `NumberFormatException` (not the `IllegalArgumentException` at the loop's end); Kotlin `toInt()` throws the identical type. Test asserts `NumberFormatException.class` specifically.
- **MINOR 4 FIXED — `parseWithDefault` nullability.** Added an explicit Unknown 4 (extended) Resolved Decision: both params non-null (`value: String, defaultValue: SortOrder`), reasoned from the real body (`valueOf(value)` already NPEs on null name; `defaultValue` only returned, never dereferenced) and the sole caller `UserPreferences.java:829` (passes non-null both), with the future-null-`defaultValue` residual flagged as currently unreachable — not a J2K rubber-stamp.
- Nothing red-team explicitly verified-and-approved was re-litigated (TextUtils identity swap, two-commit sequencing, 4-file batch scope, no-Robolectric, `MediaMetadataRetrieverCompat` no-unit-test exception, the other 3 nullability decisions all left intact).

## Decisions from red-team review, loop 2 (2026-07-21)
- **Verdict: APPROVE (final loop).** All four loop-1 fixes independently re-verified against current real source (not just the planner's description):
  1. **MAJOR 1 fix confirmed correct** — `VolumeAdaptionSetting.java:16-17` re-read directly, confirmed `private`; Step 5/Resolved Decisions/AC now correctly specify `private val` (no `@JvmField`); `VolumeAdaptionSettingTest.java` re-read directly, confirmed it never touches the raw fields.
  2. **MAJOR 2 fix confirmed correct** — ran `javap -p` against the project's real `compileSdk 36` `android.jar`, confirmed `MediaMetadataRetriever implements AutoCloseable` with a real non-final `close()` — `override` is genuinely mandatory; Step 6 now includes it.
  3. **MINOR 3 fix confirmed correct** — re-read `SortOrder.java:64`, confirmed `Integer.parseInt` runs before the unknown-code `IllegalArgumentException` loop, so `NumberFormatException` is the correct propagating type; new test is correctly targeted.
  4. **MINOR 4 fix confirmed correct** — re-read `UserPreferences.java:828-829` directly, confirmed it is the sole caller and passes non-null for both `parseWithDefault` params, matching the new Resolved Decision.
- Full pass over the rest of the Plan found no new inconsistencies introduced by the fixes. Full findings in `## Red-Team Verdict — Plan` (loop 2 entry) in the task file.
- Plan is locked. Next step is implementation.

## Decisions from implementation (2026-07-21)
- All 7 Steps implemented in order, each verified build-green before moving to the next: (1) `MediaTypeTest.java` (8 methods) + `MediaType.java` `TextUtils`→pure-Java guard swap, still Java, green on bare JVM; (2) `MediaType.kt` conversion, same 8 tests unchanged and green; (3) `SortOrderTest.java` (11 methods) + `SortOrder.java` guard swap, still Java, green; (4) `SortOrder.kt` conversion, same 11 tests unchanged and green; (5) `VolumeAdaptionSetting.kt` conversion, existing 13-method `VolumeAdaptionSettingTest.java` unchanged and green (no test edit, as planned); (6) `MediaMetadataRetrieverCompat.kt` with mandatory `override fun close()`, no unit test (documented exception); (7) `model/README.md` updated with the 4 new Kotlin files and the no-unit-test-exception note.
- Confirmed `:model`'s actual build variants before starting: plain `debug`/`release` build types, no product flavors (`./gradlew :model:tasks --group verification`), so `:model:test` (per `AGENTS.md`) correctly runs both `testDebugUnitTest` and `testReleaseUnitTest`.
- `./gradlew --console=plain :model:test` run after every step (baseline, and after Steps 1 through 6) — all green, 268 test methods / 0 failures at final state.
- `./gradlew :app:assembleDebug` run after Steps 4, 5, and 6 as the downstream-Java-caller gate — all green, confirming no public API break for the ~23/~37/~15/~5 downstream callers of the four converted files, and specifically confirming the mandatory `override` on `MediaMetadataRetrieverCompat.close()` compiles clean against the real `compileSdk 36` `AutoCloseable`/`MediaMetadataRetriever` supertype.
- `./gradlew checkstyle lint` run as the final pre-PR style gate per `AGENTS.md` — `BUILD SUCCESSFUL`, exit 0.
- No deviations from the approved Plan. Field visibility, `@JvmField`/`@JvmStatic` placement, the `override` modifier, the guard-decouple two-commit sequencing, and the no-unit-test exception for `MediaMetadataRetrieverCompat.close()` all match the Plan's Resolved Decisions exactly. Diff confirmed via `git status` to be scoped to exactly the File Scope list (4 `.java` deletions, 4 `.kt` creations, 2 new test files, `model/README.md` edit) plus this session's own task/checkpoint files.
- Full Implementation Notes (commit message draft, per-test before/after table, test commands) are in `## Implementation Notes` in the task file. Acceptance Criteria checkboxes in the task file's `## Plan` section are all checked off.
- Changes are staged/unstaged in the working tree — no commit was created (not requested).

## Decisions from code review, loop 1 (2026-07-21)
- **Verdict: REQUEST CHANGES.** Full findings in `## Code Review Verdict` in the task file. I independently re-verified (not just re-read the Implementation Notes' self-report): re-ran `./gradlew :model:testDebugUnitTest --tests MediaTypeTest --tests SortOrderTest --tests VolumeAdaptionSettingTest --rerun` (33/33 PASSED), re-ran `./gradlew :app:assembleDebug` (green), diffed each new `.kt` file against `git show HEAD`'s original `.java` for field visibility, numeric literals, and nullability (all correct — the loop-1/loop-2 red-team fixes for `VolumeAdaptionSetting` field visibility and `MediaMetadataRetrieverCompat`'s `override` were genuinely implemented, not just claimed), and read every test method's assertions (all assert real values/exception types, none are bare invokes).
- **1 MAJOR finding — real `ktlint` failure.** `:model` applies the `ktlint` plugin and `ktlintMainSourceSetCheck` is wired into `:model:check` (confirmed via `--dry-run`, not skipped). Running `./gradlew :model:ktlintMainSourceSetCheck` fails with 3 `standard:enum-wrapping` violations: `MediaType.kt:4` (`AUDIO, VIDEO, UNKNOWN;` on one line) and `SortOrder.kt:26` (nested `Scope` enum on one line). This module already has an established one-entry-per-line precedent (`FeedOrder.kt` from Milestone 1, which passes ktlint cleanly) that the new files don't follow. `AGENTS.md`'s literal pre-PR command (`./gradlew checkstyle lint`) does NOT invoke ktlint, so the developer's "PASS" report for that command was accurate but incomplete — it didn't cover this module's real ktlint gate, which would fail CI as-is.
- Fix needed: reformat `MediaType.kt`'s top-level enum and `SortOrder.kt`'s nested `Scope` enum to one entry per line (matching `FeedOrder.kt`), then verify with `./gradlew :model:ktlintMainSourceSetCheck` (or `:model:check`) before re-requesting review.
- Nothing else needs to change — scope, field visibility, `@JvmStatic`, nullability, numeric contracts, `override`/no-`@Throws`, test quality, and `model/README.md` are all correct as implemented.

## Decisions from ktlint fix, post-review-loop-1 (2026-07-21)
- Applied the single MAJOR fix from code review loop 1: reformatted `MediaType.kt`'s top-level enum (`AUDIO, VIDEO, UNKNOWN;` → one entry per line, trailing `;` on its own line before `companion object`) and `SortOrder.kt`'s nested `Scope` enum (`INTRA_FEED, INTER_FEED` → one entry per line, no `;` needed — no companion object in `Scope`), both mirroring `FeedOrder.kt`'s exact established formatting. No other lines in either file touched, no other files touched.
- Verified: `./gradlew :model:ktlintMainSourceSetCheck` — now PASS (was FAIL with 3 `standard:enum-wrapping` violations). `./gradlew --console=plain :model:test` — re-run, full suite PASS (0 failures), confirming the pure-formatting change is behaviorally inert. `./gradlew :app:assembleDebug` — re-run, `BUILD SUCCESSFUL`; only pre-existing `D8: Unexpected error during rewriting of Kotlin metadata` warnings present (across many unrelated classes too, not introduced by this change, and warnings not errors per `AGENTS.md`).
- Task file's `## Implementation Notes` (Test commands run, Deviations from plan) updated to record this fix. Nothing else in Implementation Notes changed — the rest of loop 1's REQUEST CHANGES review confirmed everything else correct, so it was left untouched per instruction not to re-litigate.
- No commit created (not requested).

## Decisions from code review, loop 2 (2026-07-21)
- **Verdict: APPROVE.** No CRITICAL/MAJOR findings open. Full findings in `## Code Review Verdict` (loop 2 entry) in the task file.
- Independently re-verified the loop-1 ktlint fix against live source, not the developer's self-report: read `MediaType.kt`/`SortOrder.kt` directly and confirmed both enums now match `FeedOrder.kt`'s one-entry-per-line precedent exactly; forced `./gradlew :model:ktlintMainSourceSetCheck --rerun` (genuinely executed, not served from cache) — PASS; `ls -laT` mtimes confirmed only `MediaType.kt`/`SortOrder.kt` were touched in the fix (nothing else in File Scope re-touched); re-ran targeted tests (`MediaTypeTest`/`SortOrderTest`/`VolumeAdaptionSettingTest`, all pass) and cross-checked the fresh JUnit XML for `MediaTypeTest` directly (8/8, 0 failures); re-ran full `:model:test` and `:app:assembleDebug` — both green.
- Milestone 3's code-review gate is now closed. Next lifecycle step is `legacy-android-red-team` reviewing the running implementation (diff-level review, distinct from the plan red-team already done).

## Decisions from red-team review, implementation loop 1 (2026-07-22)
- **Verdict: APPROVE (loop 1 of max 2).** Full findings in `## Red-Team Verdict — Implementation` in the task file. Independently re-read all four `.kt` files against `git show HEAD`'s original `.java` (not just re-checking code review's checklist), specifically hunting for the two named idiom traps from Milestone 2:
  1. **`String.split()` trailing-empty-token trap — does not apply.** Grepped all four converted files for `split`; zero matches, no split-like operation exists in this batch.
  2. **`serialVersionUID`/`Serializable` exposure trap — does not apply.** Confirmed via `git show HEAD` that none of the four original `.java` files implemented `Serializable`/`Parcelable`, and confirmed none of the four new `.kt` files picked up either.
- Sampled real downstream call sites for `SortOrder` (~37 refs) and `MediaType` (~23 refs) beyond what code review checked — `SingleFeedSortDialog.java`, `UserPreferences.java` (including the `parseWithDefault` call site), `FeedItemPermutors.java`/`FeedItemSortQuery.java`, `LocalFeedUpdater.java`, `LocalPSMP.java`, `PlayActionButton.java` — all field reads/static calls/`==`/`.equals()` comparisons, behaviorally proven equivalent by source-level diff, not just compile success. Found and verified one additional live cross-reference not called out by prior reviews: `model/src/main/java/de/danoeh/antennapod/model/feed/Feed.java:462` (still-Java) reaches into the nested `SortOrder.Scope` enum (`sortOrder.scope != SortOrder.Scope.INTRA_FEED`) — confirmed Kotlin's nested `enum class Scope` compiles to the same `SortOrder$Scope` shape Java's nested enum did, so this keeps compiling/behaving identically.
- Confirmed `TextUtils.isEmpty` → `isNullOrEmpty()` (not `isNullOrBlank()`) via direct grep of all four `.kt` files, and traced the actual Kotlin stdlib contract enabling the smart-casts after the guard — a real compiler-verified narrowing, not an unsound cast. Null, empty-string, and whitespace-only inputs all behave identically pre/post-conversion.
- Confirmed `MediaMetadataRetrieverCompat.close()`'s `override` is a logic-for-logic match against the original Java (`release()` wrapped in `catch (IOException)`, swallowed, no rethrow either side) — the `override` keyword addition is a Kotlin compile-time requirement only, not a runtime behavior change. Confirmed `model/README.md`'s no-unit-test-exception note is honest ("verified by inspection," not overstated as "tested").
- Independently re-ran the test suite myself (forced non-cached `--rerun`, not trusting the Implementation Notes/Code Review self-reports): `MediaTypeTest` 8/8 pass (cross-checked fresh JUnit XML), `SortOrderTest` 11/11 pass (cross-checked fresh JUnit XML), `VolumeAdaptionSettingTest` 14/14 pass.
- **One MINOR, non-blocking finding:** the task file has stated `VolumeAdaptionSettingTest` has "13 methods" since the Research section, repeated uncritically through Plan/Implementation Notes/Code Review — actual count (verified via `grep -c "@Test"` and by running the class) is **14**. Zero equivalence impact; flagged so the stale count doesn't keep propagating if this task file is referenced again.
- Milestone 3's implementation red-team gate is now closed. Next lifecycle step is PR opened, using the Plan section as the PR description per the standard workflow.

## Resume command
Open the PR for Milestone 3. Plan, Code Review (loop 2 APPROVE), and Red-Team Implementation (loop 1 APPROVE) are all closed — see `## Red-Team Verdict — Implementation` in the task file and `## Decisions from red-team review, implementation loop 1` above. The diff to include is exactly the File Scope list: `MediaType.kt`, `SortOrder.kt`, `VolumeAdaptionSetting.kt`, `MediaMetadataRetrieverCompat.kt` (all new, replacing their deleted `.java` originals), `MediaTypeTest.java`, `SortOrderTest.java` (new characterization tests), and `model/README.md` (edit). No commit has been created yet — create a new branch (never commit directly to `develop`), commit using the message drafted in `## Implementation Notes` in the task file, then open the PR against `develop` on `josegbel/antenna-pod-kt` following `.github/pull_request_template.md` per `AGENTS.md`'s PR Conventions. Use the Plan section as the PR description per the standard AEPM lifecycle (step 8).
