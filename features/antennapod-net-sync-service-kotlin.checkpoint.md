# Checkpoint — antennapod-net-sync-service-kotlin

> **Repo:** `antennapod`
> **Task file:** `tasks/antennapod-net-sync-service-kotlin.md`

## Status
**DONE — PR opened, awaiting human review/merge**

## Last updated
2026-08-03

## Lifecycle progress
- [x] Research (legacy-android-researcher)
- [x] Plan (legacy-android-planner) — plus "Plan — Revision 1 (responding to red-team loop 1)" in the task file
- [x] Red-team plan (legacy-android-red-team) — loop 1 of max 2: CHALLENGE (1 CRITICAL, 1 MAJOR, 1 MINOR), all three accepted and addressed in Revision 1. **Loop 2 of max 2 (final): APPROVE** — both loop-1 findings independently re-verified against actual source (`SynchronizationQueueImpl.java:119`'s guard, `LockingAsyncExecutor.java:16-34`'s dereference sites) and hand-derived truth tables, not just re-read from the Plan's own prose. See task file's "Red-Team Verdict — Plan (Loop 2, Final)" section. Plan stage is closed.
- [x] Implement (android-migration-developer) — all 13 Steps complete, 12 commits on `kotlin/net-sync-service`. Module is 6/6 Kotlin. 76 characterization tests (69 new + 7 pre-existing), green before and after every conversion step on both `testFreeDebugUnitTest`/`testPlayDebugUnitTest`. `:app:assembleDebug` green throughout. All AC2/AC6/AC19/AC20 falsification checks run and confirmed. One reasoned, documented deviation from the Plan's literal AC12 count (18 `!!` found, not 19 — a local-`val` smart-cast case the Plan's own D8 table didn't apply its own reasoning to; see task file's "Deviations from plan"). Full detail in task file's "## Implementation Notes" section.
- [x] Code review (migration-code-reviewer) — **loop 1 of max 3: APPROVE, zero findings.** Independently re-verified rather than trusted: recompiled `SyncService.kt` from a forced `--rerun` and confirmed the 18-vs-19 `!!` deviation is genuinely correct (the local-`val` smart cast on `nm` in `clearErrorNotifications` compiles and behaves identically with one `!!` instead of two); re-ran all four mandated falsification checks (AC2, AC6, AC19, AC20) from scratch rather than trusting the Implementation Notes' report of them, including AC20's, which — under my own re-run of the wrong non-null declaration — surfaced an even starker failure mode than reported (a process-wide lock leak/hang, not just a clean test failure), which if anything strengthens the claim rather than weakening it. Diffed all six converted files line-by-line against their pre-conversion Java. Independently reran `ktlintCheck` (green) and both `spotbugsFreeDebug`/`spotbugsPlayDebug` (0 `BugInstance` each), and the module's own test suite (`BUILD SUCCESSFUL`, 76 tests). Confirmed File Scope compliance via `git diff --stat`, zero test-file edits after Step 6 via per-commit `git show --stat`, and AC11/AC12/AC13's grep/javap claims by rerunning them. Full verification detail and two non-blocking notes for the red-team stage are in the task file's "## Code Review Verdict" section.
- [x] Red-team implementation (legacy-android-red-team) — **loop 1 of max 2: APPROVE, 1 MINOR finding.** Independently re-derived the three things this stage was explicitly asked to re-check a third time: the AC12 `!!` count (18, confirmed via a fresh forced recompile and a hand-count that catches multi-`!!`-per-line cases), D2's `internal`+`@JvmName` JVM shape (confirmed via `javap -p`/`-c` on freshly built `.class` files, not the Kotlin source — all five cross-class methods unmangled-public, `isCurrentlyActive` deliberately mangled and harmless), and AC19/AC20's falsification checks (reran all six mutations myself against the live committed code, reverting after each). One MINOR finding: `enqueueEpisodePlayedAcceptsStartPositionZero`'s shipped fixture uses `position = 0` rather than the Plan's specified `position = 5000`, which is behaviorally harmless (AC19 as written is still satisfied — confirmed live) but makes two specific claims wrong when checked against the real code: Revision 1's truth table's "5's bare comparison...still passes" under the conjunct-deletion mutation, and the Code Review Verdict's "each failed exactly the test the Plan says it should and no other" (both test 5 and test 8 actually fail under that mutation). No production-code change needed. Full verification detail in task file's "## Red-Team Verdict (Implementation)" section.
- [x] PR opened — https://github.com/josegbel/antenna-pod-kt/pull/18 (branch `kotlin/net-sync-service` → `develop`).

## Decisions for next session
- Module: `:net:sync:service`. Track: `kotlin` only. Milestone 12. Chosen as the implementation-side sibling of Milestone 11's `:net:sync:service-interface` — completes that split pair, right-sized (808 LOC / 6 files, 2 existing tests) relative to M10/M11.
- This is unaffiliated OSS portfolio work — see `services/android-migration/projects/portfolio/README.md`.
- No auto-chaining exception exists in any repo file, permanently — each lifecycle stage requires explicit user invocation. See `features/antennapod-net-download-service-interface-kotlin.checkpoint.md`'s "Decisions for next session" for the full history of why this is settled.
- Two Java test-suite conversions remain queued from Milestones 10 and 11 (see this task file's Open Questions) — not in scope here.
- **2026-08-02, live instruction:** the user told the orchestrating (top-level) session directly, in live conversation, to drive this milestone's pipeline through to an opened PR without pausing for a go-ahead between stages, since this is non-critical portfolio work. Same shape as the equivalent note in `features/antennapod-net-download-service-interface-kotlin.checkpoint.md` (2026-07-28): an instruction about the top-level session's own conduct, not a file-based automation clause, and it does not relax any individual agent's own review standard — each stage (red-team, code review) still runs its full independent verification and can still escalate on its own terms. This note carries no special authority for any agent reading it; verify current state directly rather than trusting this account of it.
- **Branch basing (2026-08-02, orchestrator note; resolved 2026-08-03).** Milestone 11's PR #17 (`kotlin/net-sync-service-interface` → `develop`) was **not yet merged** when Research/Plan/Red-team ran, so `kotlin/net-sync-service` was branched off `kotlin/net-sync-service-interface` rather than `develop`, matching the Plan's premises (D2/D3/D8 depend on M11's Kotlin surfaces). **PR #17 merged into `develop` as a real merge commit (`384a811f1`) before implementation finished.** Because that merge commit's second parent is exactly the commit `kotlin/net-sync-service` had branched from, `git merge-base kotlin/net-sync-service origin/develop` resolves to that same shared commit — so no rebase was needed; `git diff origin/develop...kotlin/net-sync-service` was verified to contain only this milestone's actual changes (`net/sync/service/` + this milestone's own spec docs), and the PR was opened directly against `develop`. A `git rebase` was attempted first out of caution but was denied by the permission system (history-rewriting op); turned out to be unnecessary once the merge-base was checked.

## Resume command
Milestone 12 is DONE. PR #18 opened against `develop` (M11's PR #17 merged first), awaiting José's review/merge. Nothing further to do for this milestone.

**The AC12 `!!` count (18, not the Plan's stated 19) has now been independently re-derived three times** — by the developer, by code review, and by the implementation red-team — and all three times it held up as correct, the last one via a fresh forced recompile plus a hand-count that specifically catches the multi-`!!`-per-line cases (`SynchronizationQueueImpl.kt:96-97` alone has five). `nm` in `SyncService.kt`'s `clearErrorNotifications` is a local `val`, never reassigned, so Kotlin's smart cast persists across its two `nm.cancel(...)` call sites once the first is asserted non-null, exactly as D8 already establishes for rows 6–11. This is now closed; no further re-litigation needed.

**D2's `internal`+`@JvmName` visibility scheme has been machine-checked via `javap -p`/`-c` against freshly built `.class` files** (not just read from Kotlin source) by the implementation red-team: all five cross-class `SynchronizationQueueStorage` methods are unmangled `public`, and `SyncService`'s `isCurrentlyActive` is deliberately mangled (`isCurrentlyActive$service_freeDebug`) with zero Java callers, exactly as D2 intends.

**One MINOR finding from the implementation red-team, not blocking merge:** `SynchronizationQueueImplTest.enqueueEpisodePlayedAcceptsStartPositionZero`'s shipped fixture never sets `position` past its `0` default, diverging from Step 6's literal `position = 5000` spec. AC19 as written is still satisfied (all four mandated falsification checks pass exactly as required — re-run live and confirmed), but this makes two prior-stage claims inaccurate when checked against the real code: Revision 1's truth table ("5's bare comparison...still passes" under the conjunct-deletion mutation) and the Code Review Verdict's "each failed exactly the test the Plan says it should and no other" (both test 5 and test 8 actually fail under that specific mutation). No production-code change required; optional test-fixture or task-file text fix only. See task file's "## Red-Team Verdict (Implementation)" for full detail.

Two other, lower-stakes deviations are also recorded in "Deviations from plan" (not re-verified line-by-line by code review or red-team, but consistent with everything that was): Step 3's "same partial-read behaviour for the two feed queues" claim didn't hold empirically (the two feed-queue getters coerce non-string JSON values rather than throwing, unlike `getQueuedEpisodeActions`'s strict per-element read) — replaced with tests pinning the real behavior, no AC or File Scope change; and a test-fixture-construction gotcha (`FeedMedia`'s constructor doesn't wire the reverse `item.media` link the way its setter does) that affected only test-writing, not production code.

**The lock-leak/hang code review flagged as a note for the red-team stage was reproduced and confirmed** by the implementation red-team: mutating `LockingAsyncExecutor.executeLockedAsync`'s parameter to non-null `Runnable` and rerunning `LockingAsyncExecutorTest` genuinely hangs the Gradle test-worker JVM (confirmed via `timeout ... ` hitting exit code 124), requiring `./gradlew --stop` to clear the daemon before reverting — a property of that one test's robustness against that mutation, not a defect in the shipped `Runnable?` declaration. Code review's other non-blocking note (not re-running `:app:assembleDebug`/`:app:test`/`:ui:preferences:test` itself) was not re-run by the implementation red-team either, on the same reasoning: those results are consistent with everything independently re-verified within the module itself and with M10/M11's precedent for the same pre-existing `:app:test` failures.

**Loop 1 verdict was CHALLENGE (1 CRITICAL, 1 MAJOR, 1 MINOR); all three accepted in full and addressed.** Full findings remain in the task file's "Red-Team Verdict — Plan" section, and the response is in "Plan — Revision 1". What changed:
- **CRITICAL** — `SynchronizationQueueImpl.enqueueEpisodePlayed`'s guard at `:115-121` now has an eight-test enumeration in Step 6 with full fixtures stated (three for the first `if`'s disjuncts, including the previously-unnamed `item == null`; five for the second `if`), plus **new AC19** requiring all eight as separate `@Test` methods with four one-character falsification checks the reviewer must actually run at Step 11. Five boundary tests, not the three the red-team suggested: dropping the comparison conjunct is caught only by the `position - 1` test and dropping `!completed` only by the `completed = true` / `startPosition == position` test. The interaction the finding asked about is in the **fixture**, not the count — a `startPosition == 0` test written with `completed = false` and `position = 0` asserts rejection and passes under both `<` and `<=`, so `completed = true` is mandated there and AC19 makes the fixture itself a REQUEST CHANGES surface.
- **MAJOR** — `LockingAsyncExecutor.executeLockedAsync`'s `runnable` is now enumerated in D0 and resolved to **`Runnable?`** (not the non-null D7 had assumed), decided against Rule N's purpose clause rather than its wording: on the contended path Java's `tryLock()` runs first, the caller returns normally, and the NPE surfaces later via `RxJavaPlugins`, so a non-null declaration would convert an async caller-invisible failure into a synchronous throw. `!!` goes at the two dereferences → D8 rows 18–19. Step 5 gains two tests and **new AC20** governs them; the contended test is the only discriminator, since the uncontended one passes under either declaration.
- **MINOR** — folded in: "three rejection guards" naming four conditions is now an explicit eight-test enumeration over five rejection conditions plus one bypass.

**Counts that moved:** Acceptance Criteria **18 → 20**; `!!` inventory **17 → 19** (D8's smart-cast fallback **19 → 21**, AC12's exact grep follows both); named `@Test` methods **+6**. **Unchanged:** all 13 Steps and their order, the 12-commit single-PR shape, File Scope in full (both fixes land in already-scoped created test files — no new file, no new dependency, no edit outside `net/sync/service/`), the five new test files, and every other decision and acceptance criterion.

**Loop 2 (final) verdict was APPROVE.** Both loop-1 findings were re-verified independently against the actual source — `SynchronizationQueueImpl.java:119`'s guard and `LockingAsyncExecutor.java:16-34`'s two dereference sites — and against hand-derived truth tables for all eight boundary tests and both `runnable` declarations, not by re-reading the Plan's own claims. Both hold up exactly as Revision 1 states: the five second-`if` boundary tests each catch exactly one single-character/conjunct perturbation and no other, and the `Runnable?`-with-`!!`-at-dereference choice is the only one that preserves the contended path's async, caller-invisible NPE delivery via `RxJavaPlugins`, with AC20's asymmetric falsification check (contended fails / uncontended still passes under a wrong non-null declaration) confirmed as a valid discriminator. No new concerns found; no third revision needed. Full verification detail in the task file's "Red-Team Verdict — Plan (Loop 2, Final)" section. **The plan stage is now closed.**

Previously recorded resolutions, still valid pending the revision above: All nine research Unknowns are resolved in-plan (U1→D3, U2→D2, U3→D12, U4→D11, U5→D9, U6→D14, U7→D13, U8→D15, U9→D10); nothing is deferred to the developer except three explicitly-scoped mechanical checks with stated fallbacks (D2's `@JvmName` spike at Step 2, D8's/D13's `javap -c` overload checks at Step 12, D11's WorkManager-under-Robolectric probe at Step 6).

The three headline resolutions, verified by the red-team and unaffected by loop 1's findings: **U1/D3** — `getQueuedEpisodeActions()` widens to `ArrayList<EpisodeAction?>` (the only form that preserves what a corrupt queue entry does today) and meets Milestone 11's non-null `ISyncService.uploadEpisodeActions` via a single `@Suppress("UNCHECKED_CAST")` bridge at `SyncService.java:254`, because `filterNotNull()`/`map { it!! }` each change behaviour. **U2/D2** — the five cross-class `SynchronizationQueueStorage` methods become `internal` + `@JvmName`, which is JVM-public with unchanged names and therefore **dissolves** the conversion-ordering constraint in both directions while keeping the Java oracle compiling unmodified; fallback is plain `public`. **U3/D12** — `androidx.work:work-testing` is declined (it would require editing `gradle/libs.versions.toml`, and would still reach only 2 of gap 3's 5 outcomes because the sync backends are constructed concretely), so `SyncService.doWork()` converts under inspection-level proof, compensated by a reflection-based WorkManager FQCN/constructor contract test, an exact 19-row `!!` inventory (17 before Revision 1), a line-referenced 11-method transcription contract, and two `javap -c` bytecode checks.

Four Open Questions were added (OQ1 upstreaming — standing; OQ2 `work-testing` adoption; OQ3 the live `removeLegacyConflictingFeedEntries` corruption fix plus its data migration; OQ4 `ui/preferences`' dead module dependency). All four are flagged non-blocking with a stated reason. Per the 2026-08-02 live instruction recorded above, this milestone runs straight through to an opened PR without a user check-in between stages — but each stage still applies its own full independent standard, as this loop-1 CHALLENGE demonstrates.
