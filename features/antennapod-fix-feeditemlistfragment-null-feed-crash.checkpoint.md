# Checkpoint — antennapod-fix-feeditemlistfragment-null-feed-crash

> **Repo:** `antennapod`
> **Task file:** `tasks/antennapod-fix-feeditemlistfragment-null-feed-crash.md`
> **Sibling tasks:** `antennapod-fix-spotbugs-static-analysis-debt` (merged, PR #23), `antennapod-fix-net-download-service-spotbugs-debt` (merged, PR #25), `antennapod-fix-app-wearos-compile-error` (merged, PR #26), `antennapod-fix-httpdownloadertest-null-bundle-argument` (research done). This is the fifth CI-blocker/bug discovered in the same investigation chain — the first that is a genuine production defect rather than a stale test or a compile/lint artifact.
> **Blocks:** Milestone 15b (`tasks/antennapod-sync-settings-before-screenshot-milestone-15b.md`, PR #22) — needed alongside the `HttpDownloaderTest` sibling task for a *reliably* (not retry-luckily) green `emulator-test` run for AC13.

## Status
**IMPLEMENTATION: Steps 1-2 done (2026-08-18), Steps 3-4 outstanding.** `android-migration-developer` implemented the characterization test and the D1 fix as two separate commits on `fix/feeditemlistfragment-null-feed-crash` (base re-verified at `8443366a412326673dd4e626a62ff89fd9a2670c`, unchanged from plan time). No device/emulator was available in this environment, so the two `FeedItemlistFragmentTest` methods are compile-verified before and after the fix but not yet runtime-verified — Steps 3a/3b (CI runs) and Step 4 (recording the CI evidence table) are explicitly deferred to whichever stage next opens the PR, per this session's own instructions not to push or open it. See the task file's **Implementation Notes** section for the full commit list, test-command log, diff-inspection verification of every Step-2 Acceptance Criterion, and the process-deviation note. History below predates implementation and is unchanged from the prior session:
- Loop 1 (2026-08-14, CHALLENGE): the Plan's "naive guard would crash on the main thread" justification was factually wrong (`Observable`'s `LambdaObserver` routes a thrown consumer NPE to the existing graceful error handler — no crash in any build type). Fix shape unchanged (`Maybe.fromCallable` + null guard), D1-D3 reasoning and one AC corrected → rev. 2.
- Loop 2 (2026-08-14, APPROVE, run in a prior session): found and required a MINOR prose-precision fix only (D1 mislabeling the rejected "naive" variant as Research's actual Option (A)); independently re-verified the four-variant UI-equivalence table and the 2-arg-`subscribe` silent-drop AC against real RxJava 3.1.5 source and found both accurate. **This review did not check `MaybeCallbackObserver` (the class that actually backs the fix's 3-arg `Maybe.subscribe`) against `LambdaObserver` — it assumed analogous behavior rather than verifying it.**
- Loop 2-rerun (2026-08-17, CHALLENGE, final — this session): independently re-derived from source rather than trusting the 2026-08-14 APPROVE block sitting in the file, and caught what that review missed. **CRITICAL:** `MaybeCallbackObserver.onSuccess`'s catch (unlike `LambdaObserver.onNext`'s) routes straight to `RxJavaPlugins.onError`, not to the user's own error consumer. Combined with `loadItems()`'s shared, unsynchronized `feed` field and Research's Verification 3 (disposing an in-flight call doesn't stop its computation-thread worker), a live invocation's success consumer can still read a field nulled out by a slower, disposed invocation finishing later — reopening the exact undeliverable-exception crash path this task exists to close, just via a rarer race.
- José chose the fix (2026-08-17): close the gap in File Scope — success consumer reads the `Maybe`'s emitted result instead of the shared `feed` field (or a defensive null-check as its first line). No File Scope change.
- **Plan rev. 3 (2026-08-17, `legacy-android-planner`) — applied.** Of the two variants José offered, the Plan adopts the **defensive null-check as the success consumer's first statements** (`if (feed == null) { adapter.setDummyViews(0); adapter.updateItems(Collections.emptyList()); return; }`), not the `result.first` read — a recorded, reasoned divergence from his stated leaning under his "make the call and record why" delegation. Decisive reason: the consumer has **five** feed-dependent reads, not three; the two inside `refreshHeaderView()`/`updateToolbar()` can't move off the field without editing code outside `loadItems()`, so `result.first` would move only part of the consumer and could render a hybrid of two different feeds, while one guard keeps it internally consistent. It is a three-line swap if José prefers the other variant. Changed in rev. 3: D1 (new `MaybeCallbackObserver` bullet, new code block, variant rationale), D2 point 1 (structural proof extended to both ends, three bounds), D2 risk table (third row), D3 (note: guard is dead code in the single-invocation scenario, so the four-variant table stands), Step 2 (four enumerated changes), three ACs revised + four added, Out of Scope (one clause + a "do not make the field thread-safe" bullet), D4 (non-binding note only — approved wording untouched). Research, File Scope, the CI bar and OQ-1..OQ-4 untouched.

**Bookkeeping note:** the Loop 2 (2026-08-14) verdict block existed in the task file before this session started, but this checklist and the old Status text never reflected it — they still said "ready for Loop 2." Root cause not investigated (likely a prior session ending before checkpoint sync ran); flagging in case it recurs elsewhere in this portfolio's task files.

## Last updated
2026-08-18

## Lifecycle progress
- [x] Research (legacy-android-researcher) — 2026-08-14
- [x] Plan (legacy-android-planner) — 2026-08-14, rev. 2
- [x] Red-team plan (legacy-android-red-team) — Loop 1 CHALLENGE 2026-08-14, Loop 2 APPROVE 2026-08-14 (incomplete — missed the MaybeCallbackObserver gap), Loop 2-rerun CHALLENGE 2026-08-17 (final; CRITICAL finding, fix direction chosen, plan rev. 3 pending)
- [x] Plan rev. 3 (legacy-android-planner) — 2026-08-17, folds in the MaybeCallbackObserver fix per José's chosen direction; **final plan version, no Loop 3**
- [~] Implement (android-migration-developer) — 2026-08-18, Steps 1-2 (test + fix, separate commits) done; Steps 3-4 (CI verification, requires PR) outstanding, deferred to the PR-opening stage
- [ ] Code review (migration-code-reviewer)
- [ ] Red-team implementation (legacy-android-red-team)
- [ ] PR opened (also needed to complete Steps 3-4 of the Plan: CI verification)
- [ ] PR merged
- [ ] Milestone 15b's branch rebased/re-checked once more for a genuinely, reliably green `emulator-test` run

## Decisions for next session
- Discovered 2026-08-13 by `legacy-android-researcher`, while researching the sibling `antennapod-fix-httpdownloadertest-null-bundle-argument` task, via a controlled comparison across 7 CI attempts of the same commit (3 API levels × up to 3 retries) — `DownloadLogTest.testExistingNonSubscribedFeed` passed in 4/7, failed in 3/7, independent of the other bug in that CI run.
- Root cause already diagnosed by the parent research pass (not yet independently re-verified by this task's own researcher): `FeedItemlistFragment.java:659` dereferences `DBReader.getFeed(...)`'s `@Nullable` return with no null check, on an RxJava computation thread; when null, the resulting NPE becomes RxJava-undeliverable on a disposed subscriber and crashes the app process.
- José authorized this 2026-08-13 as a fifth, independent follow-up task, run through the full pipeline — this is a genuine production null-safety defect (not test staleness), so it gets the same rigor as the other tasks despite being a small, well-localized fix.
- This is flaky/probabilistic in the instrumented-test manifestation (depends on test-class execution order and timing), which will make the equivalence proof and verification approach genuinely harder than the other four tasks in this chain — flag this early rather than discover it mid-implementation.

## Research findings that change the task's shape
- **Line 659 confirmed unchanged.** `DBReader.getFeed`'s `@Nullable` is a genuine, documented invariant (`DBReader.java:321-353`) and must **not** be tightened — 9 of 15 production call sites already guard it.
- **CORRECTION 1 — the prior diagnosis was wrong that there is no `onError` handler.** One exists at `FeedItemlistFragment.java:676-683` and already handles the null-feed case gracefully. The crash is specifically RxJava's **disposed-subscriber** branch bypassing it.
- **CORRECTION 2 — severity overstated.** `RxJavaErrorHandlerSetup.java:16-36` (installed at `PodcastApp.java:19`) swallows this exact exception class; it only rethrows under `if (BuildConfig.DEBUG)` (`:31-34`). **Release builds do not crash.** They write a phantom `crash-report.log` that AntennaPod shows the user in its Bug Report screen (`BugReportViewModel.java:66-67`). PR wording must not say "fixes a production crash" — flagged for José (OQ / Unknown 4).
- **A naive null guard at `:659` alone is a REGRESSION.** The success consumer reads the `feed` *field*, not `result.first` (`:666,667,671`); guarding only the callable makes it NPE on the **main thread**, which crashes in every build type. The fix must address both ends.
- **Production trigger confirmed real** (softening not needed on this point): `MainActivity.loadChildFragment` uses `.hide().add()` not `replace`, so a hidden `FeedItemlistFragment` stays EventBus-registered and `onQueueChanged`/`onPlayerStatusChanged` re-trigger `loadItems()` unconditionally; feeds vanish via `SyncService.kt:164` (cross-device unsubscribe) and `FeedUpdateWorker.java:115` → `NonSubscribedFeedsCleaner`.
- **Test trigger pinned deterministically:** JUnit `MethodSorters.DEFAULT` hash ordering puts `testExistingMedia` immediately before `testExistingNonSubscribedFeed`, and `testExistingMedia` is what opens the stale `FeedItemlistFragment`.
- **Zero test coverage of `FeedItemlistFragment`** of any kind — no JVM test, no targeted instrumented test. `:app` has Robolectric 4.16 but **no** `fragment-testing`/`FragmentScenario`.
- **Static analysis structurally cannot catch this:** `config/spotbugs/exclude.xml:250` globally suppresses `NP_NULL_ON_SOME_PATH_FROM_RETURN_VALUE` (upstream, ByteHamster 2021-03-27). Out of scope to change.
- **New CI evidence:** run `31796405935` reports `Emulator Test API 30` as **`success`** while its log contains a full signature-matched crash. Job conclusions are not a valid signal — verification must grep logs for `FeedItemlistFragment.lambda$loadItems`. Also confirms independence from the merged `HttpDownloaderTest` fix (`There was 1 failure(s)`).

## Plan decisions (2026-08-14) — what the next agent must not relitigate
- **D1 Fix shape = `Maybe.fromCallable` + `if (feed == null) return null;` + an `onComplete` handler mirroring the existing error consumer minus `Log.e`.** The `Maybe` switch is *not* the fix — the null short-circuit is; `Maybe`'s null→`onComplete` contract is what makes the guard safe with no downstream null hazard. ~~Closes both ends by construction: the success consumer (which reads the `feed` *field* at `:666,667,671`) is never invoked when the feed is missing.~~ **Superseded by rev. 3 (2026-08-17): that "both ends by construction" claim was false** — a cross-invocation field clobber can still invoke the live success consumer with `feed == null`, and `MaybeCallbackObserver` escalates the resulting NPE to `RxJavaPlugins.onError` instead of the graceful error consumer. **D1 now specifies a fourth change: a null guard as the success consumer's first statements.** Zero import churn, changes still confined to `loadItems()`.
- **D2 Verification bar, split deliberately.** Primary = structural proof (the callable can no longer throw for this cause, in any build type / disposal state) — bounded: does not cover other throwables from `DBReader`. Secondary = deterministic instrumented test for the live-subscriber path. Corroboration = **3 full `emulator-test` runs, ≥9 clean attempt-legs, zero raw-log occurrences of `FeedItemlistFragment.lambda$loadItems`**, via `gh run rerun` (no junk commits). Residual stated honestly: ~1.9% at p̂=0.36, up to ~26% at the pessimistic 95% lower bound. **CI job conclusions are never evidence.** Legs cancelled by the missing `fail-fast: false` don't count toward the sample.
- **D3 Characterization: Robolectric NO, instrumented YES.** Research only evaluated the Robolectric option (agreed: `fragment-testing` File-Scope cost not justified, and it can't reach the disposed path). It did not evaluate the instrumented option, which needs **zero new dependencies** and is **fully deterministic** — `FeedSettingsTest.setUp()`'s `EXTRA_FEED_ID` launch pattern against a nonexistent feed id in a cleared DB. Passes pre-fix, passes post-fix, and **fails hard on the naive-guard landmine**. A stress/loop test to force the disposal race was explicitly rejected (would add a flake to the job this task exists to stabilize).
- **D4 PR wording is fixed verbatim in the Plan** — "debug-build crash + release-build phantom crash report", never "production crash", plus an explicit "this does not by itself make `emulator-test` reliably green". Needs José's sign-off (OQ-1) before the PR opens.
- **D6 Base = `origin/develop` @ `8443366a4`** (includes merged PRs #23, #25, #26, #27). Branch `fix/feeditemlistfragment-null-feed-crash`. Current working branch `docs/httpdownloadertest-checkpoint-final` is NOT the base.
- **D7/D8 deferred, not dropped:** `loadMoreItems():701` and the four other unguarded `getFeed` sites. D7's exposure is provably unchanged by this fix (`feed == null` either way), so deferral is safe, not just convenient. Placement is OQ-2.
- **Step 1 is the characterization test and must be a separate commit from Step 2**, so the "green BEFORE" evidence is recordable — locally if an emulator is available, otherwise from the Step-1-only CI run (expect that run to still go red from the very crash being fixed; that is not the criterion).

## Resume command
Plan is final at **rev. 3** — see the Plan section of `tasks/antennapod-fix-feeditemlistfragment-null-feed-crash.md` (Objective / Resolved Decisions D1-D10 / 4 Steps / File Scope / Acceptance Criteria / Milestone / Out of Scope / **Plan status**), plus OQ-1..OQ-4 in Open Questions. The plan-review stage is **closed**: both red-team loops are spent and the CRITICAL finding from the second was resolved by José's direct decision, folded in as rev. 3.

**Implementation status (2026-08-18):** Steps 1-2 are done on `fix/feeditemlistfragment-null-feed-crash` — commit `675d2a0fe` (characterization test, `FeedItemlistFragmentTest.java`, no production code) and commit `d91e6a484` (the D1 fix in `loadItems()`, four changes exactly as specified). Both compile clean (`:app:assembleDebug`, `:app:assemblePlayDebugAndroidTest` before and after the fix, `checkstyle lint`, `:app:testPlayDebugUnitTest`, all BUILD SUCCESSFUL — see task file's Implementation Notes for the full command log). **No device/emulator was available locally**, so the two characterization test methods are compile-verified only, not runtime-verified, in either state.

Next step: **open the PR** (this was explicitly out of scope for the implementation session) and complete **Steps 3a/3b/4 of the Plan** — record the Step-1-commit-only "green BEFORE" evidence from CI raw logs (or accept the compile-only evidence already on record if the reviewing stage judges that sufficient), then push Step 2 and collect 3 full `emulator-test` runs (`gh run rerun`, ≥9 clean attempt-legs) grepping raw job logs for zero occurrences of `FeedItemlistFragment.lambda$loadItems`, per D2's bar. After that, `migration-code-reviewer` should review the diff against the Plan (the task file's Implementation Notes already contain a full diff-inspection walkthrough of every Step-2 Acceptance Criterion to check against), followed by `legacy-android-red-team` on the implementation. Do **not** weaken `RxJavaErrorHandlerSetup`, tighten `DBReader.getFeed`, un-suppress the SpotBugs pattern, fold in the four other unguarded `getFeed` call sites or `loadMoreItems():701`, add any new user-visible string, convert anything to Kotlin/Coroutines, or try to make the `feed` field genuinely thread-safe (no `volatile`/`synchronized`/`AtomicReference`, no per-invocation `Disposable`) — rev. 3 makes the read *safe*, and the underlying design smell is OQ-2 future work.
